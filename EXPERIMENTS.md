# Append-Only Experiment Ledger

Historical entries below must never be edited or deleted. Corrections are new entries that cite the superseded entry.

## Literature additions

- ADD-REF-DOREMI — DoReMi: Optimizing Data Mixtures Speeds Up Language Model Pretraining. Reason: foundational comparator explicitly required by the task and directly attacked by Aioli and Olmix. Evidence: `scripts/reference_inventory.py`; input `references/doremi_2305.10429.pdf`; command `python scripts/reference_inventory.py --references references --vendor vendor --output artifacts/reference_inventory.json`.
- ADD-REF-DOGE — DoGE: Domain Reweighting with Generalization Estimation. Reason: foundational gradient-based mixing method explicitly required by the task and directly analyzed by Aioli and Olmix. Evidence: `scripts/reference_inventory.py`; input `references/doge_2310.15393.pdf`; command `python scripts/reference_inventory.py --references references --vendor vendor --output artifacts/reference_inventory.json`.
- ADD-REF-DATADECIDE — DataDecide: How to Predict Best Pretraining Data with Small Experiments. Reason: released multi-scale, multi-seed results enable zero-GPU tests of mixing transfer assumptions. Evidence: `scripts/reference_inventory.py`; input `references/datadecide_2504.11393.pdf`; command `python scripts/reference_inventory.py --references references --vendor vendor --output artifacts/reference_inventory.json`.
- ADD-REF-ADMIRE — ADMIRE-BayesOpt: Accelerated Data Mixture Re-weighting for Language Models with Bayesian Optimization. Reason: it is the closest uncertainty-aware sequential alternative to the multi-fidelity mixing framework and directly tests whether cross-scale proxy choice improves target mixture search. Evidence: `scripts/reference_inventory.py`; input `references/admire_bayesopt_2508.11551.pdf`; command `python scripts/reference_inventory.py --references references --vendor vendor --output artifacts/reference_inventory.json`.
- ADD-REF-TIKMIX — TiKMiX: Take Data Influence into Dynamic Mixture for Language Model Pre-training. Reason: it is the closest dynamic influence-based comparator for GRAPE, DoGE, and RegMix-D assumptions about gradient or trajectory sufficiency. Evidence: `scripts/reference_inventory.py`; input `references/tikmix_2508.17677.pdf`; command `python scripts/reference_inventory.py --references references --vendor vendor --output artifacts/reference_inventory.json`.
- ADD-REF-MERGEMIX — MergeMix: Efficient Data Mixture Optimization via Model Merging. Reason: it is the closest model-merging proxy prior to OP-MIX and is necessary to assess novelty of adapter-interpolation mixing. Evidence: `scripts/reference_inventory.py`; input `references/mergemix_2601.17858.pdf`; command `python scripts/reference_inventory.py --references references --vendor vendor --output artifacts/reference_inventory.json`.
- ADD-REF-INTERNAL-REPETITION — Internal Data Repetition Destroys Language Models. Reason: it provides the closest adverse evidence for repetition-aware mixing laws and tests whether repetition topology, not only repetition count, controls degradation. Evidence: `scripts/reference_inventory.py`; input `references/internal_repetition_2606.24998.pdf`; command `python scripts/reference_inventory.py --references references --vendor vendor --output artifacts/reference_inventory.json`.

## REVIEW-CALIBRATION

- Date: 2026-08-24.
- Blindness: reviewer launched with `fork_turns=none` and received only two one-page calibration hypotheses plus the reference filename list.
- CAL-RM raw scores: novelty 6, falsifiability 9, impact 8; oral-level REJECT. The three closest works were Data Mixing Laws, DoReMi, and DoGE; strongest objections were prior overlap, global-rank versus top-region validity, and transported baselines.
- CAL-DG raw scores: novelty 6, falsifiability 8, impact 7; oral-level REJECT. The three closest works were DoReMi, Skill-It, and Learning to Reweight Examples; strongest objections were local versus long-horizon utility, gradient-norm confounding, and narrow empirical breadth.
- Calibration action: because both published core hypotheses fell below the threshold of 7 only on novelty, subsequent reviews use a novelty offset of 1. The substantial-overlap veto and thresholds of 7 for all calibrated dimensions remain unchanged.
- Budget action: conservative token accounting forced relative-selection mode before candidate review because cumulative use 386781 exceeded the review-generation pool limit 200000 under the total budget 500000.
- Evidence: `scripts/check_review.py` and `scripts/check_budget.py`; inputs `reviews/calibration.json`, `protocol.json`, `artifacts/budget_snapshot_20260824.json`; commands `python scripts/check_review.py reviews/calibration.json --threshold 7 --novelty-offset 1` and `python scripts/check_budget.py --protocol protocol.json --snapshot artifacts/budget_snapshot_20260824.json`.

## JOB-384355 — L0 environment setup

- Date: 2026-08-24. Slurm state `COMPLETED`, elapsed `00:00:43`, exit code `0:0`, requested nodes `1`, requested CPUs `4`, MI210 GPU count `0`, and MI210 node-hours `0`.
- Output versions: NumPy `2.5.2`, pandas `3.0.5`, SciPy `1.18.1`, scikit-learn `1.9.0`.
- Evidence: `slurm/setup_l0_env.sbatch`; inputs `slurm/setup_l0_env.sbatch` and the public Python package index; commands `sbatch slurm/setup_l0_env.sbatch` and `sacct -j 384355 --format=JobID,State,Elapsed,ExitCode -n -P`; raw output `/work1/ruixiangtang/rw761/data_mix_artifacts/slurm/setup_l0_env-384355.out`.

## BUDGET-CORRECTION-001

- Date: 2026-08-24. This entry supersedes only the budget action in `REVIEW-CALIBRATION`; it does not alter that historical entry or the completed review.
- The user raised the total token budget from `500000` to `50000000`. The generation-and-review pool is therefore `20000000` tokens (`50000000 * 0.4`), while the captured cumulative use was `477562`; the conservative cutoff check returns `forced_relative_selection=false`.
- Consequence: absolute review resumes for new candidates. The already completed relative-selection result for H002/H003/H006 remains recorded and may be tested because it was valid under the budget in force when issued.
- Evidence: `scripts/check_budget.py`; inputs `protocol.json`, `artifacts/budget_snapshot_20260824_after_raise.json`; command `python scripts/check_budget.py --protocol protocol.json --snapshot artifacts/budget_snapshot_20260824_after_raise.json`; output `artifacts/budget_status_20260824_after_raise.json`.

## H001 v1 — REJECTED

- ID/date/type/level: H001, 2026-08-24, measurement, L0.
- One-page statement: RegMix can have high global proxy-to-target rank agreement while failing in the decision-relevant Pareto region. This attacks A001, A009, and A027. The original papers report global rank/prediction transfer but do not test the conjunction below. The public 64-mixture Pile grid was to use IDs 0–31 for discovery and 32–63 for confirmation, with uniform mean validation loss as the frozen target.
- Falsifier and minimum detectable effect: support required top-quartile overlap below `0.50` and selected target regret above `2` domain-bootstrap sigma in both halves; overlap at least `0.50` or regret at most `2` sigma would falsify. The minimum overlap deficit was `0.125`; `10000` domain-stratified bootstrap replicates were proposed. Cost was zero GPU.
- Why proposed as new: RegMix reports global Spearman; the page attempted to separate global prediction from local decision quality.
- Review conclusion: raw scores N/F/I `5/6/7`, calibrated `6/6/7`; REJECT, substantial-overlap veto, unresolved fatal objection, not fallback eligible.
- Nearest prior work and differences: RegMix reports global Spearman on the same swarm, whereas H001 targeted top-quartile overlap and regret; DataDecide uses pairwise decision accuracy over 25 recipes, 14 sizes, and three seeds, whereas H001 targeted an extreme region; *Can Small Training Runs Reliably Guide Data Curation?* already centers top-k decision regret, leaving mainly a dataset/statistic change; ADMIRE-BayesOpt uses prospective uncertainty, whereas H001 was retrospective.
- Three strongest objections: the statement required high global correlation but set no Spearman threshold; domain resampling is not target-training seed noise; and the two ID halves were not shown exchangeable while overlap moves in `0.125` increments. The first is a fatal statement-to-test mismatch.
- Disposition: abandoned without test. The closest 2026 prior creates substantial overlap, so repair is not eligible.
- Evidence for every count, threshold, and score in this entry: `scripts/check_review.py`; inputs `reviews/candidate_round1.json`, `references/regmix.pdf`, `references/datadecide_2504.11393.pdf`, and the linked prior metadata stored in the review; command `python scripts/check_review.py reviews/candidate_round1.json --threshold 7 --novelty-offset 1`; output `artifacts/candidate_round1_check.json`.

## H002 v1 — PREREGISTERED, admitted-by-fallback

- ID/date/type/level: H002, 2026-08-24, measurement, L0. Attacks A017 and A024.
- One-page statement: under the published additive data-mixing scaling law, the point optimum is not statistically identified: resampling mixture blocks produces materially separated optimal weight vectors whose target losses are practically indistinguishable on one common, untouched upper-scale criterion. The source paper reports point optima and predictive error but does not report an argmin confidence set.
- Why previously untested: Data Mixing Laws reports fitted surfaces, the multi-fidelity framework represents acquisition uncertainty, and Explaining DMSL reports point optima; none reports bootstrap set-identification for the frozen Apple additive law on this released SlimPajama table.
- Frozen falsifier and minimum detectable effect: support requires all three conditions: median pairwise L1 distance among bootstrap optima above `0.10`; at least one coordinate's percentile `95%` interval wider than `0.10`; and median common-evaluator loss difference among those optima at most `2` sigma. Failure of any condition falsifies H002. Use `1000` mixture-block bootstraps, with lower model sizes for generation and upper model sizes untouched until confirmation.
- Reviewer: raw N/F/I `7/6/7`, calibrated `8/6/7`; absolute REJECT with unresolved fatal objection, no overlap veto; relative rank `3`, hence admitted-by-fallback.
- Nearest priors: Scaling Laws for Optimal Data Mixtures gives the attacked point optima; Data Mixing Laws fits selected response surfaces without argmin sets; Data Mixture Optimization uses uncertainty prospectively rather than diagnosing this deterministic law; Explaining DMSL gives mechanistic point optima without confidence regions.
- Three strongest objections: per-bootstrap self-evaluation was not a common estimand; numerical optimizer dispersion could masquerade as non-identifiability; and the scale split/target/mixture-block bootstrap were underspecified.
- Frozen response in the design: every generated optimum is scored by one common upper-scale response surface fitted only after generation; each bootstrap uses `20` deterministic multistarts and must have optimizer spread at most `1e-6` in objective plus KKT/simplex residual at most `1e-6`; model-size halves, target size, mixture keys, and resampling unit are emitted in the result artifact. These are validity checks, not changed support thresholds; failing a validity check yields `inconclusive`.
- Cost: zero GPU, one Slurm CPU job. Evidence will be a JSON result plus the batch log. No result data have been inspected at preregistration time.
- Evidence for every count, threshold, score, and literature count in this entry: `scripts/check_review.py`; inputs `reviews/candidate_round1.json`, `references/scaling_laws_optimal_mixtures_2507.09404.pdf`, `vendor/ml_scalefit/examples/fit_dmsl.py`, and `vendor/ml_scalefit/data/dmsl_llm_slimpajama.csv`; command `python scripts/check_review.py reviews/candidate_round1.json --threshold 7 --novelty-offset 1`; output `artifacts/candidate_round1_check.json`.

## H003 v1 — PREREGISTERED, admitted-by-fallback

- ID/date/type/level: H003, 2026-08-24, audit, L0. Attacks A020.
- One-page statement: OP-MIX pretraining fits weights over probes trained on `0.9 e_i + 0.1 u`, but its final pretraining sampler consumes those coefficients as direct data ratios without applying or inverting the affine map `q = 0.9 a + 0.1 u`; consequently a material fraction of released candidate coordinates differ from the data coordinates actually represented by the proxy.
- Original-paper coverage: Algorithm 1 and Appendix B idealize pure-domain probes; Appendix A.2 states the `90%/10%` probe recipe. Figure 8 studies proxy regret but does not isolate this coordinate contract. Code locations are `vendor/on_policy_mix/pipeline/pretrain_opm.py:453`, `vendor/on_policy_mix/pretrain.py:321`, and `vendor/on_policy_mix/pipeline/pretrain_opm.py:248`.
- Why previously untested: MergeMix studies merge/data-rank consistency, while OLMix and RegMix use actual mixture coordinates; no nearest prior traces this affine coordinate contract through OP-MIX's final sampler.
- Frozen falsifier and minimum detectable effect: H003 is supported only if static source tracing finds no affine expansion/inversion before final sampling and, in both discovery and confirmation, the maximum candidate L1 coordinate error exceeds `1e-12` and the median is at least `0.02`. It is falsified if the final path applies the correct map or either split has maximum error at most `1e-12`; otherwise it is inconclusive. Discovery uses the paper's first fixed candidate-generation seed and `5` domains; confirmation uses a disjoint seed and a `7`-domain synthetic generalization. Continual OP-MIX is a negative control and must map its explicit ratio fields within `1e-12`.
- Noise/effect: this is deterministic contract evaluation repeated over `3` exact invocations. If all repetitions agree, sigma is `0`; a nonzero coordinate effect is reported as `infinite` sigma with its absolute L1 magnitude. Any repeat disagreement yields `inconclusive`.
- Reviewer: raw N/F/I `8/6/7`, calibrated `9/6/7`; absolute REJECT with fatal objections, no overlap veto; fallback rank `1`, hence admitted-by-fallback.
- Nearest priors: OP-Mix is the audited source; MergeMix shares merging proxies but not this affine trace; OLMix uses separately trained mixture coordinates; RegMix uses actual ratios.
- Three strongest objections and frozen response: the original universal wording was inconsistent with max/median, so the claim is limited to the declared distribution; the continual-style formula might not match pretraining, so the test derives `q = 0.9a + 0.1u` from the five-probe code; and algebra alone is insufficient, so source tracing to the final sampler is a required support condition.
- Cost: zero GPU, one Slurm CPU job. No test output was inspected before this entry.
- Evidence for every count, threshold, score, and code-line number in this entry: `scripts/check_review.py`; inputs `reviews/candidate_round1.json`, `references/op_mix_2605.15220.pdf`, and the cited `vendor/on_policy_mix` files; command `python scripts/check_review.py reviews/candidate_round1.json --threshold 7 --novelty-offset 1`; output `artifacts/candidate_round1_check.json`.

## H004 v1 — REJECTED

- ID/date/type/level: H004, 2026-08-24, audit, L0. Attacked A026 and A027.
- One-page statement: target-fidelity rows designated as ADMIRE test rows remain in the acquisition/training pool, so reported test MSE and target-search efficiency are not out of sample. The proposed trace would intersect row IDs across fitting, acquisition, and evaluation; support required nonempty overlap, contamination fraction at least `0.05`, and corrected replay changing mean MSE or target regret by at least `2` random-seed sigma. The Pile path was discovery and IFT was confirmation. Cost was zero GPU.
- Original coverage/novelty claim: ADMIRE reports multi-fidelity optimization but not a temporal row-lineage audit; earlier multi-fidelity work uses a separate simulator split.
- Review conclusion: raw N/F/I `7/5/8`, calibrated `8/5/8`; REJECT with unresolved fatal objection, no overlap veto; non-overlap fallback rank `4`, not admitted.
- Nearest prior work and differences: ADMIRE explicitly optimizes over a finite training pool; Data Mixture Optimization uses a separate 422/50 predictor split; RegMix separates regression and target mixtures; standard multi-fidelity BO legitimately acquires target-fidelity candidates after paying their cost.
- Three strongest objections: the page likely conflates finite-pool BO with supervised test leakage; row-ID intersection lacks the required temporal label trace; and changing either MSE or regret cannot establish invalidity of both, while the shared IFT pipeline is not independent confirmation.
- Disposition: abandoned without test. A repair would first need to identify a paper-claimed held-out estimand, so the current public-code observation is insufficient.
- Evidence for every count, threshold, and score in this entry: `scripts/check_review.py`; inputs `reviews/candidate_round1.json`, `references/admire_bayesopt_2508.11551.pdf`, and `vendor/admire_bayesopt/mfbayesopt_maxvalue.py`; command `python scripts/check_review.py reviews/candidate_round1.json --threshold 7 --novelty-offset 1`; output `artifacts/candidate_round1_check.json`.

## H005 v1 — REJECTED

- ID/date/type/level: H005, 2026-08-24, measurement, L0. Attacked A014 and A015.
- One-page statement: high held-out point-prediction R-squared does not certify low mixture-selection regret in the public 472-run multi-fidelity table. The proposed frozen test used leave-one-model-size-out evaluation and hashed domain halves; support required at least one scale with R-squared at least `0.90` and selected regret above `2` sigma in both halves, using `10000` mixture-block bootstrap replicates. Cost was zero GPU.
- Why proposed as new: the attacked work reports predictor fit before optimization, while the page sought a direct decision-certification test.
- Review conclusion: raw N/F/I `5/6/7`, calibrated `6/6/7`; REJECT, substantial-overlap veto, unresolved fatal objection, not fallback eligible.
- Nearest prior work and differences: Data Mixture Optimization already reports scale-varying optima and R-squared before GP decisions; RegMix links rank fit to selected mixtures; DataDecide directly uses pairwise decision accuracy; *Can Small Training Runs Reliably Guide Data Curation?* already defines top-k decision regret.
- Three strongest objections: leave-one-scale-out changes the published protocol, which includes half the target-scale runs; choosing at least one scale after inspection creates multiplicity; and hashed domain halves reuse the same models/mixtures/noise while the bootstrap does not replace target-model seeds.
- Disposition: abandoned without test because the central certification critique substantially overlaps two nearer works. Repair is not eligible.
- Evidence for every count, threshold, and score in this entry: `scripts/check_review.py`; inputs `reviews/candidate_round1.json`, `references/multifidelity_multiscale_2503.21023.pdf`, `references/datadecide_2504.11393.pdf`, and `vendor/data_recipes/results/data_mixing_runs.pkl`; command `python scripts/check_review.py reviews/candidate_round1.json --threshold 7 --novelty-offset 1`; output `artifacts/candidate_round1_check.json`.

## H006 v1 — PREREGISTERED, admitted-by-fallback

- ID/date/type/level: H006, 2026-08-24, audit, L0. Attacks A018.
- One-page statement: the literal displayed assumptions of GRAPE Theorem 2.1 admit a smooth, globally bounded-gradient target and constant source losses for which every GRAPE update is zero away from the target optimum, so the displayed global epsilon-optimal convergence conclusion does not follow.
- Original-paper coverage: GRAPE Section 2.1, Theorem 2.1, and Appendix C.1 give the convergence statement/proof; the prose immediately before the theorem mentions strong convexity, but the displayed theorem does not include it. The original paper does not test a stationary-source counterexample. The empirical update is located at `vendor/grape/src/trainer.py:816` and `vendor/grape/src/trainer.py:975`.
- Why previously untested: standard nonconvex theory distinguishes stationarity from global optimality, while DoGE and group DRO do not make this exact source-to-target bridge; no nearest work supplies a literal GRAPE counterexample.
- Frozen falsifier and minimum detectable effect: discovery uses target `f(theta)=1-exp(-theta^2)`, initialization `theta=1`, and one constant source loss. Confirmation uses the same target coordinate embedded in `7` dimensions with `7` distinct constant source values and independently implemented forward-mode automatic differentiation. H006 is supported only if every explicitly incorporated displayed assumption is satisfied, source-gradient/update norm is at most `1e-12`, and the target optimality gap exceeds `0.50` in both constructions. It is falsified if an incorporated displayed assumption fails or either update norm exceeds `1e-12`; ambiguity about incorporation yields `inconclusive`.
- Noise/effect: run each construction `3` exact repetitions. Agreement gives sigma `0`, with a nonzero optimality gap reported as `infinite` sigma; disagreement yields `inconclusive`.
- Reviewer: raw N/F/I `6/9/6`, calibrated `7/9/6`; absolute REJECT solely because impact is below oral threshold, no fatal objection for the literal theorem, no overlap veto; fallback rank `2`, hence admitted-by-fallback.
- Nearest priors: GRAPE is the audited theorem; DoGE uses alignment without the same global claim; group DRO optimizes its constituent group losses; standard smooth nonconvex theory guarantees stationarity without stronger structure.
- Three strongest objections and scope response: preceding prose invokes strong convexity, so the conclusion is limited to the literal displayed theorem; the chosen target is not strongly convex, so no claim is made about an intended corrected theorem or empirical GRAPE; and all referenced displayed definitions/assumptions must be enumerated before declaring support.
- Cost: zero GPU, one Slurm CPU job. No test output was inspected before this entry.
- Evidence for every count, threshold, score, and code-line number in this entry: `scripts/check_review.py`; inputs `reviews/candidate_round1.json`, `references/grape_2505.20380.pdf`, and `vendor/grape/src/trainer.py`; command `python scripts/check_review.py reviews/candidate_round1.json --threshold 7 --novelty-offset 1`; output `artifacts/candidate_round1_check.json`.

## H003 v1 — RESULT ATTEMPT 1

- Date/verdict: 2026-08-24, `inconclusive`. Review status remains `admitted-by-fallback`, calibrated N/F/I `9/6/7`.
- Discovery (`5` components, seed `0`, `1000` candidates): median/max L1 coordinate error `0.1699068579672292/0.19997472781501544`; all three repeat medians were exactly `0.1699068579672292`, but NumPy's sample standard deviation returned `3.3993498887762956e-17`, so the implementation did not recognize exact agreement.
- Independent confirmation (`7` components, seed `1729`, `1000` candidates): median/max L1 error `0.1775721961553033/0.19998872518171068`; three repeats gave sigma `0` and infinite-sigma effect. Both splits exceed the frozen median `0.02` and maximum `1e-12` thresholds.
- Source trace: all five booleans were true, including final direct coefficient expansion and the continual-path effective-ratio negative control. The numerical/source hypothesis conditions pass, but the preregistered repeat-validity implementation failed due solely to the discovery sigma artifact.
- Job accounting: Slurm job `384362`, state `COMPLETED`, elapsed `00:00:01`, exit `0:0`, MI210 GPUs `0`, MI210 node-hours `0`.
- Evidence: `scripts/test_h003.py` and `slurm/h003_l0.sbatch`; inputs are the three `vendor/on_policy_mix/pipeline` files listed in `/work1/ruixiangtang/rw761/data_mix_artifacts/H003/result.json`; command `sbatch slurm/h003_l0.sbatch`; raw log `/work1/ruixiangtang/rw761/data_mix_artifacts/slurm/h003-384362.out`; result `/work1/ruixiangtang/rw761/data_mix_artifacts/H003/result.json`; accounting command `sacct -j 384362 --format=JobID,State,Elapsed,ExitCode -n -P`.

## H003 v1 — RESCUE 1 PREREGISTRATION

- Date: 2026-08-24. This is rescue analysis `1` of the allowed maximum `2` for the same hypothesis/data pair. It corrects only the repeat-agreement implementation; hypothesis, splits, source assertions, and effect thresholds remain frozen.
- Correction: define repeat sigma as exactly `0` when all three stored repeat statistics compare equal, otherwise use the sample standard deviation. This matches the preregistered phrase “all repetitions agree” and prevents a floating reduction artifact from overriding bitwise-equal inputs.
- Decision rule: rerun the unchanged job. The existing supported/falsified/inconclusive logic then applies without any threshold change.
- Evidence for all counts and the observed trigger: `scripts/test_h003.py`; input `/work1/ruixiangtang/rw761/data_mix_artifacts/H003/result.json`; diagnostic command `python -c "import json; p=json.load(open('/work1/ruixiangtang/rw761/data_mix_artifacts/H003/result.json')); print(p['discovery']['repeat_medians'], p['discovery']['sigma'])"`; implementation and rerun occur only after this entry.

## H006 v1 — RESULT

- ID/date/type/level/verdict: H006, 2026-08-24, audit, L0, `supported`. Review status `admitted-by-fallback`; calibrated N/F/I `7/9/6`.
- Discovery: the one-dimensional construction satisfies every displayed Theorem 2.1 condition found by the parser. At `theta=1`, the target optimality gap is `0.6321205588285577`, source-gradient norm and update norm are `0`, and the analytic-versus-dual derivative error is `0`.
- Independent confirmation: the separately instantiated `7`-dimensional dual-number construction has the same `0.6321205588285577` gap, update norm `0`, and AD error `0`. The global gradient/smoothness bounds are `0.8577638849607068/2.0`; learning rate `0.25` is within the `0.5` bound; both regularizers are `1.0`.
- Noise/effect: three repetitions per construction give sigma `0`; the nonzero `0.6321205588285577` gap is `infinite` sigma and exceeds the preregistered `0.50` threshold. The conclusion is restricted to the literal displayed theorem.
- Job accounting: Slurm job `384364`, state `COMPLETED`, elapsed `00:00:00`, exit `0:0`, MI210 GPUs `0`, MI210 node-hours `0`.
- Evidence: `scripts/test_h006.py` and `slurm/h006_l0.sbatch`; inputs `/work1/ruixiangtang/rw761/data_mix_artifacts/paper_text/grape_2505.20380.txt` and `references/grape_2505.20380.pdf`; command `sbatch slurm/h006_l0.sbatch`; result `/work1/ruixiangtang/rw761/data_mix_artifacts/H006/result.json`; raw log `/work1/ruixiangtang/rw761/data_mix_artifacts/slurm/h006-384364.out`; accounting command `sacct -j 384364 --format=JobID,State,Elapsed,ExitCode -n -P`.

## H003 v1 — RESCUE 1 RESULT

- ID/date/type/level/verdict: H003, 2026-08-24, audit, L0, `supported`. Review status `admitted-by-fallback`; calibrated N/F/I `9/6/7`.
- Discovery (`5` components, seed `0`, `1000` candidates): median/max L1 coordinate error `0.1699068579672292/0.19997472781501544`. Three equal repeat medians give sigma `0` and an `infinite`-sigma effect.
- Independent confirmation (`7` components, seed `1729`, `1000` candidates): median/max error `0.1775721961553033/0.19998872518171068`; three equal repeats again give sigma `0` and an `infinite`-sigma effect. Both medians exceed `0.02` and maxima exceed `1e-12`.
- Source result: pretraining specifications contain only coefficient weights, the proxy reader falls back to those weights as ratios, new probes contain `0.9` new-domain mass, and final training directly expands coefficients; the continual implementation's explicit effective ratios pass the negative control.
- Job accounting: Slurm job `384365`, state `COMPLETED`, elapsed `00:00:01`, exit `0:0`, MI210 GPUs `0`, MI210 node-hours `0`.
- Evidence: corrected `scripts/test_h003.py` and `slurm/h003_l0.sbatch`; source inputs listed in `/work1/ruixiangtang/rw761/data_mix_artifacts/H003/result.json`; command `sbatch slurm/h003_l0.sbatch`; raw log `/work1/ruixiangtang/rw761/data_mix_artifacts/slurm/h003-384365.out`; result `/work1/ruixiangtang/rw761/data_mix_artifacts/H003/result.json`; accounting command `sacct -j 384365 --format=JobID,State,Elapsed,ExitCode -n -P`.

## REVIEW-CALIBRATION-ROUND-2 AND FALLBACK-12

- Date: 2026-08-24. A fresh reviewer with no round-1 context first scored the published RegMix and DoGE cores `8/9/9` and `8/9/9` for N/F/I. Because both clear `7` without approaching `10`, round 2 used no score offset.
- Round result: H007–H012 produced `0` absolute passes among `6` reviews, taking cumulative REJECT count from `6` to `12` and reflection count from `1` to `2`; either count triggers the fallback rule.
- Cumulative selection: after excluding overlap vetoes and already admitted H002/H003/H006, all remaining candidates were ranked by calibrated total, impact, falsifiability, then novelty. H004 (`21`), H011 (`21`), and H009 (`18`) are newly `admitted-by-fallback`; H007 also totals `18` but loses the impact tie `6` versus `7`. This cumulative result supersedes only the reviewer's round-local fallback ordering.
- Evidence for every score/count/rank: `scripts/check_review.py` and `scripts/select_fallback.py`; inputs `reviews/candidate_round1.json`, `reviews/candidate_round2.json`; commands `python scripts/check_review.py reviews/candidate_round2.json --threshold 7 --novelty-offset 0` and `python scripts/select_fallback.py --round reviews/candidate_round1.json --novelty-offset 1 --round reviews/candidate_round2.json --novelty-offset 0 --already-admitted H002 H003 H006 --count 3`; outputs `artifacts/candidate_round2_check.json` and `artifacts/fallback_after_12.json`.

## H007 v1 — REJECTED

- ID/date/type/level: H007, 2026-08-24, audit, L0. Attacked A016.
- One-page statement/design: the `h>=0.1` training-weight mask in Explaining DMSL omits finite target-loss cells and inflates Equation 3's Table 2 advantage. Disjoint mixture hashes were to compare the official mask with all finite nonzero cells. Support required omitted fraction at least `0.40`, unmasked-minus-masked MRE at least `0.002` and above `2` bootstrap sigma, plus advantage shrinkage at least `0.50` or a ranking reversal in both halves; `10000` mixture-block bootstraps; zero GPU.
- Review: raw/calibrated N/F/I `5/7/6`; absolute REJECT with fatal estimand mismatch, no overlap veto; cumulative fallback total `18` but not selected because H009 has impact `7` versus `6`.
- Nearest works: Explaining DMSL supplies the attacked comparison; Data Mixing Laws does not audit treatment-dependent masks; outcome-dependent-missingness theory is general rather than this benchmark instance; statcheck does not compare masked scaling-law estimands.
- Three strongest objections: refitted heldout halves do not quantify inflation of the published Table 2 estimand; equal weighting of low-weight extrapolation cells lacks scientific justification; and shrinkage/aggregation is unstable for a near-zero baseline gap.
- Disposition: abandoned without test. A single repair could exactly reproduce Table 2 and change only the mask, but it is not selected under fallback.
- Evidence for all counts, thresholds, scores, and ranking: `scripts/check_review.py` and `scripts/select_fallback.py`; inputs `reviews/candidate_round2_pages.json`, `reviews/candidate_round2.json`, `artifacts/fallback_after_12.json`; commands and outputs are recorded in `REVIEW-CALIBRATION-ROUND-2 AND FALLBACK-12`.

## H008 v1 — REJECTED

- ID/date/type/level: H008, 2026-08-24, constructive, L0. Attacked A009 and A010.
- One-page statement/design: a target-replicate `95%` confidence filter would abstain on unidentified DataDecide recipe pairs, reducing forced-decision error by at least `0.05` while retaining coverage at least `0.60`. Both metric-hash halves also required forced error at least `0.10`, selective error at most `0.05`, and reduction above `2` sigma using `10000` nested replicates; zero GPU.
- Review: raw/calibrated N/F/I `3/4/6`; absolute REJECT with unresolved fatal objection, no overlap veto; cumulative fallback total `13`, not selected.
- Nearest works: DataDecide defines the forced means; selective-prediction sets already give abstention/coverage theory; ranking-and-selection uses indifference zones; equivalence testing requires a nonzero practical interval.
- Three strongest objections: the rule uses target data and is unavailable when making the proxy decision; a zero-crossing interval is not equivalence without a practical margin; metric hashes reuse recipes, seeds, checkpoints, and correlated likelihoods.
- Disposition: abandoned without test. Repair would require both proxy-side uncertainty and a genuine indifference margin, exceeding the one-repair scope.
- Evidence for all counts, thresholds, and scores: `scripts/check_review.py`; inputs `reviews/candidate_round2_pages.json`, `reviews/candidate_round2.json`; command `python scripts/check_review.py reviews/candidate_round2.json --threshold 7 --novelty-offset 0`; output `artifacts/candidate_round2_check.json`.

## H009 v1 — PREREGISTERED, admitted-by-fallback

- ID/date/type/level: H009, 2026-08-24, measurement, L0. Attacks A012.
- One-page statement: Olmix's apparent swarm-error collapse at `K=c(m+1)` is induced by normalizing against a best member whose denominator changes with each finite sweep. Under a common independently bounded oracle, absolute heldout regret at `K=3(m+1)` grows with domain count even when the published normalized curves appear invariant.
- Original coverage/novelty: Olmix Section 3.3.2 and Figures 4/18 report normalized curves for `m={6,12,18,24}` but do not test absolute regret to one common oracle. RegMix does not vary domain dimension; optimal-mixture scaling laws do not audit this normalization; general optimization benchmarking recognizes denominator effects but has not tested Olmix's prescription.
- Frozen falsifier/minimum effect: use the official released ratios/metrics, the frozen log-linear model, the paper's three seeds, and `K=3(m+1)`. Discovery is 30M; independent confirmation is 1B. Support requires in both: normalized-regret slope magnitude at most `0.10` sigma per log2 doubling, common-oracle absolute-regret slope above `2` bootstrap sigma per doubling, and median regret at `m=24` at least `2` times `m=6`. Any missed condition falsifies. Use `10000` nested swarm-seed/heldout-mixture bootstraps.
- Review: raw/calibrated N/F/I `5/6/7`; absolute REJECT with fatal finite-oracle/dimension confounding, no overlap veto. It ranks third among remaining candidates with total `18` and is `admitted-by-fallback`.
- Three strongest objections and validity response: finite heldout oracle quality may change with dimension, so the job must bound it against a converged analytic/dense oracle within `0.10` sigma; domain sets must be verified as the paper's nested refinement or return `inconclusive`; reproducing both scales and all three seeds is required before interpreting normalization rather than model/noise artifacts. These checks do not change the support thresholds.
- Cost: zero GPU, one Slurm CPU job. Missing released tables produces BLOCKED rather than fabricated reconstruction. No outcome table has been read before this entry.
- Evidence for every count, threshold, score, and rank: `scripts/check_review.py` and `scripts/select_fallback.py`; inputs `reviews/candidate_round2_pages.json`, `reviews/candidate_round2.json`, `artifacts/fallback_after_12.json`, `references/olmix_2602.12237.pdf`; commands and outputs are recorded in `REVIEW-CALIBRATION-ROUND-2 AND FALLBACK-12`.

## H010 v1 — REJECTED

- ID/date/type/level: H010, 2026-08-24, measurement, L0. Attacked A015.
- One-page statement/design: resampling the 472 real-run blocks and refitting `200` frozen simulators would expose unstable multi-fidelity speedup. Each metric half required nominal median speedup at least `2.0` and either `95%` lower bound at most `1.0` or winner changes in at least `0.20` of worlds; three policy seeds per world, `1%` time-to-target tolerance; zero GPU.
- Review: raw/calibrated N/F/I `4/6/7`; REJECT with unresolved fatal objection and substantial-overlap veto, hence fallback-ineligible.
- Nearest works: the attacked multi-fidelity paper supplies the simulator; simulation uncertainty analysis, Kriging/bootstrap surveys, and ranking under input uncertainty already propagate metamodel uncertainty to optimizer outcomes.
- Three strongest objections: the statement was conjunctive while the support rule used OR; bootstrap worlds were not calibrated as equally admissible; metric halves shared runs, architecture, pool, and policies.
- Disposition: abandoned without test due substantial methodological overlap. A local repair cannot clear the veto.
- Evidence for every count, threshold, and score: `scripts/check_review.py`; inputs `reviews/candidate_round2_pages.json`, `reviews/candidate_round2.json`; command `python scripts/check_review.py reviews/candidate_round2.json --threshold 7 --novelty-offset 0`; output `artifacts/candidate_round2_check.json`.

## H004 v1 — CORRECTION AND PREREGISTRATION, admitted-by-fallback

- Date/type/level: 2026-08-24, audit, L0. This entry supersedes the “not admitted” disposition in H004 v1 after cumulative fallback selected H004 with calibrated total `21`; the old review remains unchanged.
- Frozen statement after reviewer scope check: ADMIRE's code labels target-fidelity rows as `X_test_high/y_test_high` for an MSE curve while those rows remain eligible for sequential acquisition. This makes that curve progressively partly in-sample if and only if the paper or released output presents it as held-out evaluation; finite-pool target acquisition itself is not called leakage.
- Test/falsifier: trace row IDs and time of label acquisition for Pile discovery and IFT confirmation. Support requires a paper/released-result heldout-MSE claim, nonempty acquired/evaluation intersection, contaminated evaluation fraction at least `0.05`, and leave-acquired-out MSE differing from the reported-set MSE by at least `2` acquisition-seed sigma. An explicit finite-pool-only description or empty intersection falsifies the revised narrow claim; absent paper/output MSE semantics yields `inconclusive`.
- Reviewer scores/objections: raw `7/5/8`, calibrated `8/5/8`; fatal category-error objection, no overlap veto. The review required identifying a heldout estimand, tracing labels temporally rather than intersecting static universes, and separating MSE from legitimate finite-pool regret. All three are now validity/decision conditions, with no relaxed numerical threshold.
- Design/cost: `20` fixed acquisition-order seeds in each dataset; discovery and confirmation use different public corpora but the result will state that shared code is mechanistic rather than independent-code replication. Zero GPU, one Slurm CPU job. No replay or MSE output was inspected before this correction.
- Evidence for every number, score, and rank: `scripts/select_fallback.py` and `scripts/check_review.py`; inputs `reviews/candidate_round1.json`, `reviews/candidate_round2.json`, `artifacts/fallback_after_12.json`, `references/admire_bayesopt_2508.11551.pdf`, `vendor/admire_bayesopt/mfbayesopt_maxvalue.py`; commands/outputs are recorded in `REVIEW-CALIBRATION-ROUND-2 AND FALLBACK-12`.

## H011 v1 — PREREGISTERED, admitted-by-fallback

- ID/date/type/level: H011, 2026-08-24, audit, L0. Attacks A019.
- One-page statement: even under the strong-convexity reading of GRAPE Theorem 2.2, monotonic raw target-loss variance does not follow without a source-target progress condition. A source mixture can drive parameters toward a point at which variance between strongly convex target losses increases at every finite iteration.
- Original coverage/novelty: Theorem 2.2 claims eventual monotonic reduction; Appendix C.2 adds an unproved positive covariance and transfers normalized to raw variance through a time-varying range. Gradient Surgery, Gradient Coordination, and MTL transfer theory discuss alignment but do not give this GRAPE raw-variance counterexample.
- Frozen falsifier/minimum effect: discovery uses `s(theta)=(theta-1)^2`, targets `(theta-1)^2+2` and `(theta+1)^2+2`, `theta_0=0`, and `gamma=0.1`. It must derive `theta_t=1-0.8^t` and variance `4 theta_t^2`, with increment positive for every finite `t` and first-step increase above `0.10`. Confirmation uses `7` distinct source losses with the same gradient, a `7`-dimensional embedding, `7` target centers, and independent forward-mode AD. Required checks are `mu=2`, `L=2`, invariant-trajectory gradient bound, formula/update error at most `1e-12`, and three exact repeats with sigma `0`. A failed assumption or nonpositive symbolic increment falsifies; ambiguous theorem quantification yields `inconclusive`.
- Review: raw/calibrated N/F/I `6/8/7`; absolute REJECT with a fatal scope objection, no overlap veto. It ranks second among remaining candidates with total `21` and is `admitted-by-fallback`.
- Three strongest objections and validity response: globally bounded gradients conflict with unconstrained global strong convexity, so the job must quote whether the theorem quantifies over all parameters or only stochastic iterates; singleton-source scope must be present, and confirmation therefore uses seven sources as a nondegenerate check; the multidimensional AD construction and formulas must be fully emitted. No empirical GRAPE claim is made.
- Cost: zero GPU, one Slurm CPU job. No computation was performed before this entry.
- Evidence for every count, threshold, score, and rank: `scripts/check_review.py` and `scripts/select_fallback.py`; inputs `reviews/candidate_round2_pages.json`, `reviews/candidate_round2.json`, `artifacts/fallback_after_12.json`, `references/grape_2505.20380.pdf`; commands and outputs are recorded in `REVIEW-CALIBRATION-ROUND-2 AND FALLBACK-12`.

## H012 v1 — REJECTED

- ID/date/type/level: H012, 2026-08-24, audit, L0. Attacked A019.
- One-page statement/design: identically labeled method-task cells in GRAPE main Table 2 and Appendix Table 9 differ beyond rounding. Odd task columns were discovery and even columns confirmation. Support required at least `3` mismatches above `0.10` in each split and one above `0.50`, with captions establishing identical protocol and three deterministic extraction repeats; zero GPU.
- Review: raw/calibrated N/F/I `3/9/4`; absolute REJECT because novelty and impact are below `7`, no fatal objection for the narrow identity claim, no overlap veto; cumulative fallback total `16`, not selected.
- Nearest works: GRAPE is the source; statcheck, TAB-AUDIT, and PaperAudit-Bench address broader inconsistency or fabrication detection but not this exact join.
- Three strongest objections: visible labels may hide checkpoint/seed/version differences; a discrepancy does not identify the correct table or reverse a conclusion; odd/even columns do not create independent evidence.
- Disposition: abandoned without test. A broader systematic audit would be a different hypothesis rather than a local repair.
- Evidence for every count, threshold, and score: `scripts/check_review.py`; inputs `reviews/candidate_round2_pages.json`, `reviews/candidate_round2.json`; command `python scripts/check_review.py reviews/candidate_round2.json --threshold 7 --novelty-offset 0`; output `artifacts/candidate_round2_check.json`.

## H004 v1 — RESULT

- ID/date/type/level/verdict: H004, 2026-08-24, audit, L0, `falsified`. Review status `admitted-by-fallback`; calibrated N/F/I `8/5/8`.
- Discovery: Pile contains `64` high-fidelity rows; the code makes `48` acquisition-eligible and `16` evaluation-only. Across `20` fixed acquisition-order seeds, every intersection count is `0` and maximum contamination fraction is `0`.
- Independent confirmation: IFT contains `76` high-fidelity rows; `60` are eligible and `16` are evaluation-only. All `20` intersections are again `0`, with contamination fraction `0`.
- Source trace: preparation initially concatenates high rows, but the actual call slices both features and labels by `[:-(X_train_high.shape[0]-threshold)]` before acquisition. This missed final consumer falsifies the leakage hypothesis.
- Job accounting: Slurm job `384368`, state `COMPLETED`, elapsed `00:00:02`, exit `0:0`, MI210 GPUs `0`, MI210 node-hours `0`.
- Evidence: `scripts/test_h004.py` and `slurm/h004_l0.sbatch`; inputs listed in `/work1/ruixiangtang/rw761/data_mix_artifacts/H004/result.json`; command `sbatch slurm/h004_l0.sbatch`; result `/work1/ruixiangtang/rw761/data_mix_artifacts/H004/result.json`; raw log `/work1/ruixiangtang/rw761/data_mix_artifacts/slurm/h004-384368.out`; accounting command `sacct -j 384368 --format=JobID,State,Elapsed,ExitCode -n -P`.

## H011 v1 — RESULT

- ID/date/type/level/verdict: H011, 2026-08-24, audit, L0, `supported`. Review status `admitted-by-fallback`; calibrated N/F/I `6/8/7`.
- Discovery: the one-source, two-target construction has `theta_t=1-0.8^t` and target-loss variance `4*theta_t^2`. Its first-step variance increase is `0.15999999999999992`; the emitted factorization certifies a positive increment at every finite iteration.
- Independent confirmation: the `7`-source, `7`-target, `7`-dimensional construction has source-gradient mixture spread `0`, forward-mode update error `0`, and minimum analytic variance derivative `1.3333333333333333` over the invariant interval. The minimum sampled increment over `1000` floating-point iterations is `-8.881784197001252e-16`, so the verdict uses the preregistered analytic derivative rather than a rounded late-iteration difference.
- Assumption/scope checks: `mu=2`, `L=2`, `gamma=0.1<=1/L`, invariant interval `[0,1]`, trajectory gradient bound `6`; the theorem displays strong convexity and bounded gradients but no global-parameter quantifier or minimum source count.
- Noise/effect: three exact repeats give sigma `0`; the first-step increase is therefore `infinite` sigma under the preregistered exact-construction convention.
- Job accounting: Slurm job `384367`, state `COMPLETED`, elapsed `00:00:01`, exit `0:0`, MI210 GPUs `0`, MI210 node-hours `0`.
- Evidence: `scripts/test_h011.py` and `slurm/h011_l0.sbatch`; inputs listed in `/work1/ruixiangtang/rw761/data_mix_artifacts/H011/result.json`; command `sbatch slurm/h011_l0.sbatch`; result `/work1/ruixiangtang/rw761/data_mix_artifacts/H011/result.json`; raw log `/work1/ruixiangtang/rw761/data_mix_artifacts/slurm/h011-384367.out`; accounting command `sacct -j 384367 --format=JobID,State,Elapsed,ExitCode -n -P`.

## H000 — REVIEW CALIBRATION ROUND 3 AND RELATIVE SELECTION

- Date: 2026-08-24. A fresh reviewer with no candidate-ledger context scored the published RegMix and DoGE cores `8/9/9` and `8/9/8` for novelty/falsifiability/impact. Both clear `7` while remaining below a uniform `10`, so the score offset is `0`.
- Absolute gate: H013 calibrated `7/8/6` and H014 calibrated `6/8/7`; both are REJECT with an unresolved fatal objection and neither has a substantial-overlap veto.
- Relative gate: because the fallback condition had already activated at `12` cumulative rejects and `2` reflection loops, the remaining non-overlap pool is ranked by total, impact, falsifiability, novelty, then ID. H014 (`21`), H013 (`21`), and H007 (`18`) are `admitted-by-fallback`; the `21` tie is resolved by impact `7` versus `6`.
- Evidence for every score/count/rank: `scripts/check_review.py` and `scripts/select_fallback.py`; inputs `reviews/candidate_round3_pages.json`, `reviews/candidate_round3.json`, `reviews/candidate_round1.json`, `reviews/candidate_round2.json`; commands `python scripts/check_review.py reviews/candidate_round3.json --threshold 7 --novelty-offset 0` and `python scripts/select_fallback.py --round reviews/candidate_round1.json --novelty-offset 1 --round reviews/candidate_round2.json --novelty-offset 0 --round reviews/candidate_round3.json --novelty-offset 0 --already-admitted H002 H003 H004 H006 H009 H011 --count 3`; outputs `artifacts/candidate_round3_check.json` and `artifacts/fallback_round3.json`.

## H014 v1 — PREREGISTERED, admitted-by-fallback

- ID/date/type/level: H014, 2026-08-24, constructive, L0. Attacks A-002, A-012, and A-017.
- One-page statement: ActiveSubspaceMix spends half of a fixed swarm budget estimating a mixture-loss response subspace and half covering that subspace; on unseen released-run rows it attains at least `30%` lower target regret than both Dirichlet-nearest and full-simplex maximin at the same observation count.
- Original coverage/novelty: RegMix and Olmix compare static swarm constructions; DMSL fits full-coordinate laws; sequential active-subspace design and LLM mixture BO exist separately, but the reviewer found no exact prior multiresponse ilr-simplex projected-maximin construction. The reviewer nevertheless rated the method derivative and the retrospective intervention incomplete.
- Frozen falsifier/minimum effect: hash `20%` of mixture IDs into an untouched candidate/evaluation pool. For `B in {2K,3K,4K}`, use `floor(B/2)` maximin-simplex pilots, estimate the H013 SVD subspace, and use the remainder for projected maximin. Over `1000` frozen swarm seeds, support requires median regret at least `30%` below both baselines, paired improvement above `2` seed sigma, and no method/scale with worse median regret at every budget, separately in discovery and confirmation. Any failed conjunction falsifies.
- Separation/design: discovery is RegMix `1M/60M` plus lower-size DMSL; confirmation is RegMix `1B`, upper-size DMSL, and Olmix. Candidate losses are hidden until scoring; all algorithms share pilots, coordinates, available rows, selector, and common observed-row oracle. Rank, budgets, thresholds, and tie handling remain frozen.
- Review: reviewer raw `7/9/8`, calibrated `6/8/7`; REJECT with fatal retrospective-intervention objection, no overlap veto, then fallback rank `1` with total `21`. The three objections are nearest-row support versus prospective realizability, omission of sequential BO/active-subspace baselines, and a saturated `B=2K` pilot fit. Results will therefore be limited to retrospective observation efficiency, report duplicate/coverage rates, and return `inconclusive` rather than extrapolate to new runs when support validity fails; thresholds are unchanged.
- Cost/evidence: one Slurm CPU job, zero GPU. Page/review/rank inputs `reviews/candidate_round3_pages.json`, `reviews/candidate_round3.json`, `artifacts/fallback_round3.json`; selection command and numerical output are recorded in H000 round 3. No outcome table was read before this entry.

## H013 v1 — PREREGISTERED, admitted-by-fallback

- ID/date/type/level: H013, 2026-08-24, measurement, L0. Attacks A-002, A-012, A-017, and A-024.
- One-page statement: across independently released pretraining-mixture sweeps, the identifiable vector response occupies fewer than half of the nominal `K-1` ilr directions and its leading response subspace is stable across scale; nominal domain count therefore overstates the response dimension resolved by these swarms.
- Original coverage/novelty: Data Mixing Laws reports sparse interaction coefficients, but sparsity is not low rank; vector-valued active-subspace methods define related operators outside LLM mixing; Olmix and RegMix study domain count or rank transfer without singular response dimension. The cross-method measurement remains distinct, although the reviewer rejected the original common-estimand wording.
- Frozen falsifier/minimum effect: replace zero weights by `1e-6` and close, transform to an ilr basis, center columns, and fit one unregularized least-squares response operator. Define stable rank as `||J||_F^2/||J||_2^2` and overlap as mean squared canonical correlation of the top `ceil(rank)` right singular vectors. With `10000` mixture-block bootstraps, support requires every available method's median rank fraction at most `0.50`, `95%` upper bound at most `0.60`, cross-scale overlap at least `0.80`, and deficit from `K-1` above `2` sigma. Any failed method condition falsifies; fewer than `3` scales or `2` methods is `inconclusive`.
- Separation/design: discovery uses RegMix `1M` and lower-half DMSL model sizes; confirmation uses RegMix `60M/1B`, upper-half DMSL sizes, and untouched Olmix if its released table has a compatible vector response. The result must emit singular spectra, replacements, row/cell counts, condition numbers, and exact response-column semantics.
- Review: reviewer raw `8/9/7`, calibrated `7/8/6`; REJECT with fatal response-estimand objection, no overlap veto, then fallback rank `2` with total `21`. The objections are global OLS versus average-Jacobian active subspace, stable versus numerical rank, Olmix task BPB versus domain loss, and ilr support distortion. Consequently the result may claim only the preregistered affine stable-rank measurement, must report each codomain separately, and returns `inconclusive` for cross-method support if codomains cannot be aligned; numerical thresholds are unchanged.
- Cost/evidence: one Slurm CPU job, zero GPU. Page/review/rank inputs `reviews/candidate_round3_pages.json`, `reviews/candidate_round3.json`, `artifacts/fallback_round3.json`; selection command and numerical output are recorded in H000 round 3. No response table was read before this entry.

## H007 v1 — CORRECTION AND PREREGISTRATION, admitted-by-fallback

- Date/type/level: 2026-08-24, audit, L0. This entry supersedes only H007's prior “not selected” disposition after the round-3 cumulative fallback ranked it third at total `18`; its original REJECT and objections remain unchanged.
- Frozen statement: the `h>=0.1` training-weight mask in Explaining DMSL omits finite target-loss cells and inflates the reported Equation 3 versus Equation 1 Table 2 advantage under the table's own scoring population.
- Frozen falsifier/minimum effect: first reproduce Table 2 using the official fit/score population and aggregation, then change only the evaluation mask to all finite nonzero target cells. Support requires omitted-cell fraction at least `0.40`, unmasked-minus-masked MRE at least `0.002` and above `2` mixture-block-bootstrap sigma, and Equation 3's advantage to shrink at least `0.50` or reverse in both preregistered mixture-ID halves. Use `10000` bootstraps. A reproduction miss above displayed rounding or any failed conjunction is `inconclusive`/`falsified` as specified by the emitted validity flag.
- Review/response: calibrated N/F/I `5/7/6`, fatal published-estimand mismatch, no overlap veto. The repaired execution follows the reviewer's sole permitted repair by reproducing the published population before changing the mask; it also reports domain-balanced sensitivity and the masked baseline gap to expose low-denominator instability. The hypothesis wording and numerical support thresholds are not relaxed.
- Separation/cost: mixture-ID hash halves are discovery/confirmation; both reuse the deterministic public table, so the result will not call them independent training replications. One Slurm CPU job, zero GPU. No outcome table was read after fallback admission and before this entry.
- Evidence for every score/count/rank/threshold: `scripts/select_fallback.py`; inputs `reviews/candidate_round2_pages.json`, `reviews/candidate_round2.json`, `reviews/candidate_round3.json`, `artifacts/fallback_round3.json`; command is recorded in H000 round 3.

## H007 v1 — RESULT

- ID/date/type/level/verdict: H007, 2026-08-24, audit, L0, `falsified`. Review status `admitted-by-fallback`; calibrated N/F/I `5/7/6`.
- Decisive source trace: Table 2's Eq. (1) driver calls `law1`, whose metric computes the full prediction-minus-observation matrix; Eq. (3) calls `law2init`, whose evaluation comparisons are `h_set>0`. The AST audit finds no `h>=0.1` evaluation comparison in either path, so the attacked published mask does not exist.
- Magnitude/context: the released Pile table contains `62` ratio rows, `1054` finite weight cells, and `703` nonzero cells. A hypothetical `h>=0.1` rule would omit `475` nonzero cells, fraction `0.6756756756756757`, but that is not the released Table 2 evaluation rule and therefore cannot explain its advantage.
- Repetition/noise: three independent deterministic AST/data passes agree exactly; sigma `0`. The result stops before nonconvex refitting because the preregistered intervention's causal prerequisite is false, rather than substituting a new mask estimand.
- Job accounting: Slurm job `384387`, state `COMPLETED`, elapsed `00:00:02`, exit `0:0`, MI210 GPUs `0`, MI210 node-hours `0`.
- Evidence: `scripts/test_h007.py` and `slurm/h007_l0.sbatch`; inputs listed in `/work1/ruixiangtang/rw761/data_mix_artifacts/H007/result.json`; command `sbatch slurm/h007_l0.sbatch`; result `/work1/ruixiangtang/rw761/data_mix_artifacts/H007/result.json`; raw log `/work1/ruixiangtang/rw761/data_mix_artifacts/slurm/h007-384387.out`; accounting command `sacct -j 384387 --format=JobID,State,Elapsed,ExitCode -n -P`.

## H014 v1 — RESULT

- ID/date/type/level/verdict: H014, 2026-08-24, constructive, L0, `inconclusive`. Review status `admitted-by-fallback`; calibrated N/F/I `6/8/7`.
- Frozen-design feasibility: at `B=2K`, the half-budget pilot contains exactly `K` labeled rows while the ilr affine response fit has `K` parameters including its intercept, leaving residual degrees of freedom `0` in every required table. Thus the pilot cannot estimate a noise-aware active subspace under the frozen design.
- Public-support failure: RegMix 1B has `64` unique mixture rows; its frozen `20%` hash split leaves `56` unique training coordinates, below the required `4K=68`. Olmix m24 has `127` unique rows and leaves exactly `96`, meeting `4K=96`, but it retains the zero-residual-degree pilot failure.
- Decision: the preregistered `1000` performance trials executed `0` times because labels remain unread after the deterministic validity gates fail. Reporting a regret comparison would test nearest-row/support artifacts rather than the proposed intervention, so the registered validity response requires `inconclusive` rather than a performance verdict.
- Job accounting: Slurm job `384392`, state `COMPLETED`, elapsed `00:00:02`, exit `0:0`, MI210 GPUs `0`, MI210 node-hours `0`.
- Evidence: `scripts/test_h014.py` and `slurm/h014_l0.sbatch`; inputs and every table-level count are listed in `/work1/ruixiangtang/rw761/data_mix_artifacts/H014/result.json`; command `sbatch slurm/h014_l0.sbatch`; result `/work1/ruixiangtang/rw761/data_mix_artifacts/H014/result.json`; raw log `/work1/ruixiangtang/rw761/data_mix_artifacts/slurm/h014-384392.out`; accounting command `sacct -j 384392 --format=JobID,State,Elapsed,ExitCode -n -P`.

## H013 v1 — RESULT

- ID/date/type/level/verdict: H013, 2026-08-24, measurement, L0, `falsified`. Review status `admitted-by-fallback`; calibrated N/F/I `7/8/6`.
- Frozen estimator/scope: `10000` mixture-block bootstraps estimate centered global-OLS stable rank, explicitly not average-Jacobian or numerical rank. The decision uses `2` compatible domain-validation-loss methods across `58` scale tables; Olmix's `127` rows expose `110` downstream-task BPB columns and are excluded as an incompatible codomain.
- Discovery/confirmation result: DMSL's median rank fraction is `0.33564552906628536`, bootstrap `95%` upper bound `0.3390739767037292`, cross-scale overlap `0.9923385842819016`, and rank deficit `252.93840298447833` bootstrap sigma, satisfying all frozen conditions.
- Falsifier: RegMix's median rank fraction is `0.18592026636258066`, upper bound `0.22725025112635874`, and rank deficit `38.79272080379831` sigma, but its discovery-to-confirmation subspace overlap is only `0.6661252782113635` against threshold `0.80`; the bootstrap `5%` overlap bound is `0.5536742060347434`. The every-method conjunction therefore fails.
- Job accounting: Slurm job `384390`, state `COMPLETED`, elapsed `00:03:04`, exit `0:0`, MI210 GPUs `0`, MI210 node-hours `0`.
- Evidence: `scripts/test_h013.py` and `slurm/h013_l0.sbatch`; inputs, per-scale singular spectra, condition numbers, replacements, and counts are in `/work1/ruixiangtang/rw761/data_mix_artifacts/H013/result.json`; command `sbatch slurm/h013_l0.sbatch`; raw log `/work1/ruixiangtang/rw761/data_mix_artifacts/slurm/h013-384390.out`; accounting command `sacct -j 384390 --format=JobID,State,Elapsed,ExitCode -n -P`.

## H002 v1 — RESULT

- ID/date/type/level/verdict: H002, 2026-08-24, measurement, L0, `inconclusive`. Review status `admitted-by-fallback`; calibrated N/F/I `8/6/7`.
- Design execution: `1000` mixture-block bootstraps over `60` released mixture keys fit the published Apple additive form for `openhermes_loss`; each generated optimum uses `20` deterministic simplex starts. The lower generation split has `1469` rows over model sizes `122000000/310000000/680000000`; the untouched common-evaluator split has `664` rows over `937000000/1235000000/2718000000`, with target `2718000000` parameters and `103680000000` tokens.
- Threshold measurements: median pairwise optimum L1 is `0.34343846975681225`; maximum coordinate `95%` interval width is `0.5402406100477731`; median common-evaluator difference is `0.010671497152529574` against residual sigma `0.01972634031454525`, or `0.5409770379283647` sigma. All three scientific effect conditions are numerically true.
- Validity failure: only `774/1000` bootstrap parameter fits report convergence and `948/1000` optimum searches satisfy the per-bootstrap checks. Maximum multistart objective spread is `0.1216262179565577` and maximum KKT/simplex residual `4.367096134827442e-06`, both above the frozen `1e-6` limits. Numerical optimizer dispersion can therefore generate part of the apparent set width, requiring `inconclusive` despite the raw effects.
- Job accounting: Slurm job `384397`, state `COMPLETED`, elapsed `00:03:22`, exit `0:0`, MI210 GPUs `0`, MI210 node-hours `0`.
- Evidence: `scripts/test_h002.py` and `slurm/h002_l0.sbatch`; input `vendor/ml_scalefit/data/dmsl_llm_slimpajama.csv`; command `sbatch slurm/h002_l0.sbatch`; result `/work1/ruixiangtang/rw761/data_mix_artifacts/H002/result.json`; raw log `/work1/ruixiangtang/rw761/data_mix_artifacts/slurm/h002-384397.out`; accounting command `sacct -j 384397 --format=JobID,State,Elapsed,ExitCode -n -P`.

## H009 v1 — LINE STOPPED, BLOCKED

- Date/status: 2026-08-24, measurement, L0, not a completed verdict. The frozen discovery/confirmation line is stopped under public-artifact exclusion B-007.
- Artifact audit: official `m=6/12/18/24` RQ2 CSVs contain `125/130/129/127` metric rows, respectively, and `110` task columns each. Across `511` released run names, `511` contain 30M and `0` contain 1B; therefore 30M discovery is available but the required row-level 1B confirmation is absent.
- Paper/release mismatch: paper-text lines `352/354/1773/1776` describe 30M Figure 4 and 1B Figure 18, but no per-mixture/seed 1B payload exists in the official RQ2 release. Reconstructing outcomes from plotted aggregates cannot run the preregistered common oracle or `10000` nested bootstraps.
- Job accounting/evidence: Slurm job `384399`, state `COMPLETED`, elapsed `00:00:01`, exit `0:0`, MI210 GPUs `0`, MI210 node-hours `0`; `scripts/audit_h009_artifacts.py`, `slurm/h009_availability.sbatch`, inputs/results in B-007; command `sbatch slurm/h009_availability.sbatch`; accounting command `sacct -j 384399 --format=JobID,State,Elapsed,ExitCode -n -P`.

## H000 — REVIEW CALIBRATION ROUND 4 AND RELATIVE SELECTION

- Date: 2026-08-24. A new context-free reviewer scored published RegMix and DoGE cores `8/9/9` and `8/9/8`, so no global adjustment was applied.
- H015 review: raw `7/9/6`, calibrated `6/7/5`; absolute REJECT with a fatal construct-validity objection and no substantial-overlap veto. The reviewer found `4` nearest works and supplied `3` strongest objections.
- Relative selection: under the already-active fallback, H015 is the only fresh non-overlap candidate and is `admitted-by-fallback` with total `18`. The reviewer's separate “relative eligible” opinion is retained in the JSON, while the protocol's mechanical exclusion rule uses only substantial-overlap vetoes.
- Evidence for every score/count/rank: `scripts/check_review.py` and `scripts/select_fallback.py`; inputs `reviews/candidate_round4_pages.json`, `reviews/candidate_round4.json`; commands `python scripts/check_review.py reviews/candidate_round4.json --threshold 7 --novelty-offset 0` and `python scripts/select_fallback.py --round reviews/candidate_round4.json --novelty-offset 0 --already-admitted H002 H003 H004 H006 H007 H009 H011 H013 H014 --count 1`; outputs `artifacts/candidate_round4_check.json` and `artifacts/fallback_round4.json`.

## H015 v1 — PREREGISTERED, admitted-by-fallback

- ID/date/type/level: H015, 2026-08-24, measurement, L0, repair `1` of the H013 effective-dimension idea. Attacks A-012.
- One-page statement: in Olmix's official nested top-`m` RQ2 swarms, the stable rank of the affine ilr-mixture to downstream-task-BPB operator grows sublinearly in `m` and stays below half of nominal `m-1` dimension.
- Original coverage/novelty: Olmix Section 3.3.2 and Figures 4/18 report error versus `K` for `m={6,12,18,24}` but no joint task-response singular spectrum. Vector-valued dimension reduction and shared-active-subspace work define related spectral objects outside this LLM mixing benchmark.
- Frozen falsifier/minimum effect: use only finite metric columns common to all `4` releases; zero replacement `1e-6`, closure, Helmert ilr, centered raw-BPB columns, and unregularized OLS. Discovery is `m=6/12`; confirmation is untouched `m=18/24`. With `10000` mixture-block bootstraps, every `m` must have median rank fraction at most `0.50`, `95%` upper bound at most `0.60`, and nominal deficit above `2` sigma. The OLS log-log slope of stable rank versus `m-1` must be at most `0.50` with bootstrap `95%` upper bound at most `0.75`. Any failed conjunction falsifies; fewer than `100` common finite tasks or design rank below `m-1` is `inconclusive`.
- Review: raw `7/9/6`, calibrated `6/7/5`; REJECT with fatal construct-validity objection, no overlap veto, then fallback total `18`. Objections: stable rank is not statistically distinguishable dimension; task weighting/granularity defines the codomain norm; global affine projection can confound nonlinear response with support geometry.
- Frozen response/scope: the output may claim only raw-BPB global-OLS stable rank and its scaling exponent; it may not claim sample-complexity reduction, inactive directions, or an intrinsic response dimension. It emits common task names, rows, design rank, condition number, singular spectra, replacements, and separate discovery/confirmation decisions without changing thresholds.
- Cost/evidence: one Slurm CPU job, zero GPU. Page/review/rank inputs `reviews/candidate_round4_pages.json`, `reviews/candidate_round4.json`, `artifacts/fallback_round4.json`; commands/results are recorded in H000 round 4. Outcome cells were not summarized before this entry.

## H015 v1 — RESULT

- ID/date/type/level/verdict: H015, 2026-08-24, measurement, L0, `supported`. Review status `admitted-by-fallback`; calibrated N/F/I `6/7/5`.
- Scope/validity: all `4` designs have full rank `m-1` and share `110` finite raw-BPB task columns. The frozen estimator is centered global-OLS stable rank with `1e-6` zero replacement and `10000` mixture-block bootstraps; it does not estimate intrinsic or statistically distinguishable dimension.
- Discovery: at `m=6/12`, median rank fractions are `0.24628975134617892/0.11203295581408268` and `95%` upper bounds `0.25698487841694667/0.11635478771614305`; deficit effects are `125.88882256628376/375.05760668476466` bootstrap sigma.
- Independent confirmation: at untouched `m=18/24`, median rank fractions are `0.07280427145046091/0.05332770154720674`, upper bounds `0.07526819298768544/0.055949592696812045`, and deficit effects `680.9614640738205/342.73550076880593` sigma. All per-table conjunctions pass.
- Scaling condition: point stable ranks stay near `1.23`; the fitted log-log slope versus `m-1` is `-0.0030170525337915827`, bootstrap median `-0.0051797707918150426`, sigma `0.030959841084293546`, and `95%` upper bound `0.03448212813935515`, below frozen limits `0.50/0.75`.
- Job accounting: Slurm job `384402`, state `COMPLETED`, elapsed `00:00:35`, exit `0:0`, MI210 GPUs `0`, MI210 node-hours `0`.
- Evidence: `scripts/test_h015.py` and `slurm/h015_l0.sbatch`; eight official inputs and all singular spectra/counts are listed in `/work1/ruixiangtang/rw761/data_mix_artifacts/H015/result.json`; command `sbatch slurm/h015_l0.sbatch`; raw log `/work1/ruixiangtang/rw761/data_mix_artifacts/slurm/h015-384402.out`; accounting command `sacct -j 384402 --format=JobID,State,Elapsed,ExitCode -n -P`.

## H000 - REVIEW-NEIGHBOR REFERENCES ADDED

- *Gradient-Based Dimension Reduction of Multivariate Vector-Valued Functions* - added because its codomain-norm-dependent generalized spectrum is the direct mathematical ancestor of H015's vector-response operator.
- *Shared Active Subspace for Multivariate Vector-Valued Functions* - added because its shared multi-output spectra and output-normalization sensitivity are the nearest measurement prior for H015.
- *Sequential Learning of Active Subspaces* - added because its acquisition functions are the nearest adaptive-design prior for H014.
- Inventory/accounting: these `3` additions bring literature expansion to `10/15`; their page counts are `25/18/36`, respectively. Every count was produced by `scripts/reference_inventory.py` from `references/*.pdf` and `vendor/*/.git`, command `sbatch slurm/extract_review_priors.sbatch` (job `384405`), output `artifacts/reference_inventory.json`, raw log `/work1/ruixiangtang/rw761/data_mix_artifacts/slurm/extract-review-priors-384405.out`; the expansion count is `26` inventoried papers minus `16` seed papers, with seed membership recorded by the earlier H000 reference-addition entries in this append-only ledger.

## H003 - LINE CLOSED

- Status: the supported implementation-audit verdict is retained, but this research line is closed and receives no further experiment budget. Its scope remains a traced coefficient-semantics defect in one implementation, not a general data-mixing thesis claim.
- Evidence: `scripts/test_h003.py`; inputs and numeric result are recorded in `/work1/ruixiangtang/rw761/data_mix_artifacts/H003/result.json`; command `sbatch slurm/h003_l0.sbatch`; the closure decision is the user instruction following the final comparison in this thread.

## H006 - LINE CLOSED

- Status: the supported theorem-audit verdict is retained, but this research line is closed and receives no further experiment budget. Its scope remains a counterexample to the literal displayed assumptions, without a claim about repaired theory or empirical method performance.
- Evidence: `scripts/test_h006.py`; inputs and numeric result are recorded in `/work1/ruixiangtang/rw761/data_mix_artifacts/H006/result.json`; command `sbatch slurm/h006_l0.sbatch`; the closure decision is the user instruction following the final comparison in this thread.

## H011 - LINE CLOSED

- Status: the supported theorem-audit verdict is retained, but this research line is closed and receives no further experiment budget. Its scope remains a construction under the literal displayed theorem assumptions, without evidence that the mechanism occurs in pretraining.
- Evidence: `scripts/test_h011.py`; inputs and numeric result are recorded in `/work1/ruixiangtang/rw761/data_mix_artifacts/H011/result.json`; command `sbatch slurm/h011_l0.sbatch`; the closure decision is the user instruction following the final comparison in this thread.

## H000 - REVIEW CALIBRATION ROUND 5 AND RELATIVE SELECTION

- Date/calibration: 2026-08-24. The context-free reviewer calibrated on published RegMix and DoGE before reading the candidates; scores were N/F/I `8/8/9` and `8/8/8`, respectively, so the applied global offset is `0/0/0`.
- Absolute gate: H016 scored `4/6/4`, H017 scored `6/6/6`, and H018 scored `5/6/5`; all `3/3` are REJECT with unresolved fatal objections and `0/3` have a substantial-overlap veto. Each review lists at least `3` nearest works and exactly `3` strongest objections.
- Fatal findings: H016's family weighting does not remove correlated task duplication; H017's direct coefficient SVD is not prediction-loss-optimal under a non-orthogonal ilr design; H018's median-correlation gate does not validate every fitted gradient and its overlap interval conditions away affine-estimator uncertainty.
- Relative gate: the fallback condition remains active after `12` cumulative rejects and `2` reflection loops. Mechanical ranking by calibrated total, impact, falsifiability, novelty, then ID selects H017, H018, and H016 with totals `18/16/14`; none is removed by the overlap veto. H017 uses its single permitted repair before preregistration; H018 and H016 enter unchanged, with conclusions limited to their literal frozen estimands.
- Evidence for every score/count/rank: `scripts/check_review.py` and `scripts/select_fallback.py`; inputs `reviews/candidate_round5_pages.json` and `reviews/candidate_round5.json`; Slurm command `sbatch --partition=devel --nodes=1 --ntasks=1 --time=00:10:00 --wrap='<round-5 check_review and select_fallback commands>'` (job `384566`); outputs `artifacts/candidate_round5_check.json` and `artifacts/fallback_round5.json`; accounting command `sacct -j 384566 --format=JobID,State,Elapsed,ExitCode -n -P` returned state `COMPLETED`, elapsed `00:00:01`, exit `0:0`, and MI210 GPU/node-hours `0/0`.

## H000 - ROUND-5 REVIEW-NEIGHBOR REFERENCES ADDED

- *Modern Multivariate Statistical Techniques*, Section 6.3 - added because Equations 6.78-6.89 give the prediction-metric reduced-rank regression estimator that invalidates H017 v1's direct coefficient SVD; the public book treatment is used because the reviewer's original Izenman 1975 article is not available as public full text.
- *On Cross-validation for Sparse Reduced Rank Regression* - added because its foldwise projection-selection analysis directly constrains how H017 may select rank without conflating incompatible fold models.
- *Bi-cross-validation of the SVD and the Nonnegative Matrix Factorization* - added because it separates new-row prediction from new-output rank generalization; H017's repaired claim is consequently limited to fixed-task, unseen-mixture rows.
- Inventory: the new PDFs have `756/38/33` pages in the order above and bring expansion from `10/15` to `13/15`. Every page and inventory count was produced by `scripts/reference_inventory.py` from `references/*.pdf` and `vendor/*/.git`, command `sbatch --partition=devel --nodes=1 --ntasks=1 --time=00:15:00 --wrap='python scripts/reference_inventory.py --references references --vendor vendor --output artifacts/reference_inventory.json'`, output `artifacts/reference_inventory.json` (job `384574`). Relevant text was produced by `pdftotext -layout` in Slurm job `384570`, inputs `references/izenman_modern_multivariate_2008.pdf`, `references/sparse_rrr_cv_1812.11555.pdf`, and `references/bicrossvalidation_0908.2062.pdf`, outputs `/work1/ruixiangtang/rw761/data_mix_artifacts/round5_priors_text/*.txt`.
- Job accounting: `sacct -j 384568,384569,384570,384572,384574 --format=JobID,State,Elapsed,ExitCode -n -P` records fetch job `384568` as `FAILED/00:00:01/60:0` on the third URL after the first `2` downloads, retry job `384569` as `COMPLETED/00:00:02/0:0`, extraction job `384570` as `COMPLETED/00:00:25/0:0`, first inventory job `384572` as `FAILED/00:00:01/1:0` due to a missing venv module, and system-Python retry `384574` as `COMPLETED/00:00:02/0:0`; all `5` jobs requested `0` MI210 GPUs and consumed `0` MI210 node-hours.

## H016 v1 - PREREGISTERED, admitted-by-fallback

- ID/date/type/level: H016, 2026-08-24, measurement, L0. It attacks H015 and A-012.
- One-page statement: H015's affine response concentration and leading `2` mixture directions persist under raw BPB, per-task-standardized BPB, and an equal-family-quadratic-weight norm across the official Olmix top-`m` swarms. The result can establish robustness only to these `3` frozen diagonal codomain metrics; it cannot establish intrinsic dimension or removal of correlated task duplicates.
- Frozen falsifier/minimum effect: discovery is `m=6/12` and confirmation is `m=18/24`; use the same `110` common finite tasks and `10000` paired mixture-row bootstraps. Every norm and `m` must have median stable-rank fraction at most `0.50`, `95%` upper bound at most `0.60`, and rank deficit above `2` bootstrap sigma. Raw-versus-alternative rank-`2` squared canonical overlap must be at least `0.80` with bootstrap `5%` lower bound at least `0.60` at every `m`. Each norm's log-log stable-rank slope must be at most `0.50` with bootstrap `95%` upper bound at most `0.75`; any failed conjunction falsifies, while fewer than `7` tasks in a family or rank-deficient design is inconclusive.
- Frozen design: task scales are fitted inside each bootstrap. Minerva MATH is Math; HumanEval/MBPP/MT-MBPP are Code; remaining paper-listed tasks are QA. The equal-family quadratic norm divides standardized task columns by square root family size; the `3`-output family aggregate is descriptive because its algebraic rank ceiling cannot decide the hypothesis.
- Review/gate: calibrated N/F/I `4/6/4`, absolute REJECT with a fatal duplication-estimand objection and no overlap veto; fallback rank `3` with total `14` admits it unchanged. The reviewer also established that the equal-family quadratic norm is researcher-defined rather than Olmix's reported family-size-weighted objective, so any numerical support is reported using that exact name.
- Cost/evidence: one Slurm CPU job, `0` GPU. Every threshold/count/score is frozen in `reviews/candidate_round5_pages.json`, reviewed in `reviews/candidate_round5.json`, checked by `scripts/check_review.py`, and selected by `scripts/select_fallback.py`; numerical outputs are `artifacts/candidate_round5_check.json` and `artifacts/fallback_round5.json`, produced by Slurm job `384566` with the command recorded in H000 round 5. No transformed outcome or bootstrap result was read before this entry.

## H018 v1 - PREREGISTERED, admitted-by-fallback

- ID/date/type/level: H018, 2026-08-24, measurement, L0. It attacks H015 and A-012.
- One-page statement: fitting Olmix's published per-task law `f_i(p)=c_i+exp(A_i^T p)` to each official nested swarm yields a distribution-averaged nonlinear ilr Jacobian whose concentration and leading `2` mixture directions agree with H015's global affine operator. This can reject one affine-cancellation explanation only for the released proxy response; it cannot establish intrinsic dimension, target-scale transfer, or derivative truth outside observed support.
- Frozen predictive/derivative gates: use `5` SHA256 row folds and require median held-out per-task Pearson correlation at least `0.50`. Check the analytic ilr gradient `exp(A_i^T p) A_i^T(diag(p)-pp^T)B` against automatic differentiation at tolerance `1e-6`. Fit the official Huber/positive-`c` law and run `200` paired mixture-row bootstrap warm-start refits; convergence below `0.95` or a failed predictive gate is inconclusive.
- Frozen spectral falsifier: for raw and task-standardized codomain metrics form `C=mean_p J(p)^T R J(p)` and use the coefficient-equivalent stable rank `tr(C)/lambda_max(C)`; also emit the PSD-matrix stable rank `tr(C^2)/lambda_max(C)^2` only as sensitivity. Discovery is `m=6/12`, confirmation is `m=18/24`. Every norm and `m` must have coefficient-equivalent rank fraction at most `0.50` with `95%` upper bound at most `0.60`; rank-`2` squared canonical overlap with paired-refit H015 OLS must be at least `0.80` with bootstrap `5%` lower bound at least `0.60`; the log-log active-rank slope must be at most `0.50` with bootstrap `95%` upper bound at most `0.75`. Any spectral or overlap failure falsifies.
- Review/gate: calibrated N/F/I `5/6/5`, absolute REJECT with `2` fatal objections and no overlap veto; fallback rank `2` with total `16`. In response to the review, affine and nonlinear operators are paired-refit on identical bootstrap rows rather than comparing bootstrap nonlinear fits to frozen full-data OLS. The weak median-correlation derivative-validity objection remains unresolved and therefore bounds any conclusion even if the numerical conjunction passes.
- Cost/evidence: one Slurm CPU job, `0` GPU; an official-refit wall-time failure stops this L0 line rather than changing the estimator. Every threshold/count/score is from `reviews/candidate_round5_pages.json` and `reviews/candidate_round5.json`; mechanical outputs `artifacts/candidate_round5_check.json` and `artifacts/fallback_round5.json` were produced by `scripts/check_review.py` and `scripts/select_fallback.py` in Slurm job `384566`, command recorded in H000 round 5. No nonlinear fit or Jacobian outcome was read before this entry.

## H016 v1 - RESULT

- ID/date/type/level/verdict: H016, 2026-08-24, measurement, L0, `supported`. Review status `admitted-by-fallback`; calibrated N/F/I `4/6/4`, with the fatal correlated-duplication objection retained.
- Scope/validity: all `4` Olmix designs have full rank `m-1`; the same `110` finite tasks split into Math/Code/QA counts `7/19/84`. All scales were re-estimated inside each of `10000` paired mixture-row bootstraps. The result is conditional on `3` frozen diagonal codomain metrics and does not claim task de-duplication or intrinsic dimension.
- Frozen conjunction: all `12/12` norm-by-domain rank conditions and all `8/8` raw-versus-alternative rank-`2` overlap conditions pass across discovery `m=6/12` and untouched confirmation `m=18/24`. The largest rank-fraction `95%` upper bound is `0.3451602555029466`; the smallest deficit is `37.71672810247446` bootstrap sigma. The smallest rank-`2` overlap `5%` lower bound is `0.7553062395308168`, above threshold `0.60`.
- Scaling: point log-log slopes for raw/task-standardized/equal-family-quadratic norms are `-0.003017052533791688/0.22067512087085442/0.10291248631523288`; their bootstrap `95%` upper bounds are `0.03436746739867312/0.3222101440373678/0.15167507859726956`, all below threshold `0.75`.
- Job accounting: Slurm job `384588`, state `COMPLETED`, elapsed `00:01:21`, exit `0:0`, MI210 GPUs `0`, MI210 node-hours `0`; accounting command `sacct -j 384588 --format=JobID,State,Elapsed,ExitCode -n -P`.
- Evidence for every count/effect/threshold: `scripts/test_h016.py` and `slurm/h016_l0.sbatch`; eight official inputs and all per-table spectra, overlaps, task mappings, slopes, and conditions are in `/work1/ruixiangtang/rw761/data_mix_artifacts/H016/result.json`; command `sbatch slurm/h016_l0.sbatch`; raw log `/work1/ruixiangtang/rw761/data_mix_artifacts/slurm/h016-384588.out`.

## H017 v2 - REJECTED AFTER SINGLE REPAIR; LINE STOPPED

- Date/type/level: 2026-08-24, measurement, proposed L0; H017 v1 was fallback rank `1` at calibrated N/F/I `6/6/6`, then used its only permitted repair before preregistration or outcome access.
- Repair: v2 replaced direct coefficient SVD with prediction-loss-optimal RRR, moved rank selection into `4` inner folds inside `5` outer folds, specified `20` repeated cross-fits and `64` train-whitened Haar controls, and retained noninferiority/superiority margins `0.01/0.02`. The new reviewer marked the RRR algebra PASS and nested CV/random geometry PASS_WITH_RESERVATION.
- Repair review: published RegMix/DoGE calibration was `8/8/9` and `8/8/8`; H017 v2 scored N/F/I `6/4/6`, REJECT, with no overlap veto and an unresolved fatal objection. The page applies standard Helmert ilr to Olmix's exact-zero sparse weights without preregistering zero replacement or a subcomposition, so the predictor geometry and margins are undefined. Residual-only row bootstrap also fails to refit the nested learner and omits simultaneous-coverage control.
- Disposition: the single-repair allowance is exhausted. No H017 Slurm test was submitted and no outcome table was analyzed under this ID; `scripts/test_h017.py` and `slurm/h017_l0.sbatch` remain unexecuted infrastructure and must not be cited as evidence. The line is stopped rather than silently inserting its implementation's `1e-6` replacement after review.
- Evidence for every score/count/margin: `reviews/candidate_round5_repair_pages.json` and full context-free review `reviews/candidate_round5_h017_repair.json`; the latter lists `5` nearest works and `3` strongest objections. No numerical-result command or GPU accounting exists because submitted test-job count is `0`.

## H018 v2 - CORRECTION, REJECTED AFTER SINGLE REPAIR; LINE STOPPED

- Date/type/level: 2026-08-24, measurement, proposed L0. This entry corrects H018 v1's preregistration status: paired affine refits, coefficient-equivalent stable rank, and cross-fitted value/chord task gates constituted repair `1`, so the repaired design required a fresh review before testing.
- Repair review: published RegMix/DoGE calibration was N/F/I `8/9/9` and `8/9/8`; H018 v2 scored `4/2/5`, REJECT, with an unresolved fatal objection and a substantial-overlap veto. The reviewer lists `6` nearest works and `3` strongest objections.
- Fatal estimand issue: the page requires at least `90` validated "tasks," while the reviewer counts `51` paper-level standalone BPB outputs under Olmix Table 9's aggregation; the official raw RQ2 CSVs expose `110` columns because subtasks are disaggregated. The page did not resolve which unit defines task validation, so its gate is not uniquely executable. It also selects tasks using both discovery and confirmation tables before reusing those rows for operators.
- Overlap veto: the exponential mixing law, weighted vector-Jacobian Gram operator, shared active subspace, codomain normalization, and bootstrap subspace uncertainty are prior art; the remaining Olmix-specific diagnostic does not clear the oral novelty bar. Because fallback excludes overlap-vetoed candidates, the line is stopped even independently of the `90/51/110` mismatch.
- Disposition/evidence: no H018 script was written, no Slurm test was submitted, and GPU/node-hours are `0/0`. Every score/count is in `reviews/candidate_round5_h018_repair_pages.json` and the full context-free review `reviews/candidate_round5_h018_repair.json`; the raw-column count is produced by `scripts/test_h015.py` from the eight official RQ2 CSVs, command `sbatch slurm/h015_l0.sbatch`, result `/work1/ruixiangtang/rw761/data_mix_artifacts/H015/result.json`.

## H000 - LITERATURE EXPANSION CAP REACHED

- *Data Mixing Laws: Optimizing Data Mixtures by Predicting Language Modeling Performance* - added because its vector of per-validation-domain exponential mixture laws is H019's closest multi-output LLM-mixing predecessor.
- *Computing Active Subspaces with Monte Carlo* - added because its bootstrap treatment of eigenvalue and subspace uncertainty is a direct statistical predecessor of the rejected H018 design.
- Inventory/accounting: the two PDFs have `27/24` pages in the order above and bring expansion from `13/15` to the hard cap `15/15`; no further paper may be added or newly deep-read. Counts and hashes were produced by `scripts/reference_inventory.py` from `references/*.pdf` and `vendor/*/.git`; command `sbatch --partition=devel --nodes=1 --ntasks=1 --time=00:15:00 --wrap='<download, inventory, and pdftotext commands>'` (job `384592`); output `artifacts/reference_inventory.json`; extracted inputs/outputs are `references/data_mixing_laws_2403.16952.pdf`, `references/active_subspace_monte_carlo_1408.0545.pdf`, and `/work1/ruixiangtang/rw761/data_mix_artifacts/round5_priors_text/{data_mixing_laws,active_subspace_monte_carlo}.txt`.
- Job accounting: `sacct -j 384592 --format=JobID,State,Elapsed,ExitCode -n -P` records `COMPLETED`, elapsed `00:00:03`, exit `0:0`, MI210 GPUs `0`, and MI210 node-hours `0`.

## H019 v1 - REJECTED; SINGLE REPAIR REQUESTED BEFORE PREREGISTRATION

- Date/type/levels: 2026-08-24, constructive, staged L0/L1. H019 attacks A-007 and A-012 by predicting that rank-`2` response sharing can reduce an `m=6` swarm from `3(m+1)=21` to `2(m+1)=14` runs without losing final selected-mixture quality.
- Calibration/review: published RegMix and DoGE scored N/F/I `8/9/9` and `8/8/7`; H019 scored `5/5/4`, REJECT, with `5` nearest works, `3` strongest objections, unresolved fatal objections, and no substantial-overlap veto. The reviewer's fallback-ineligible opinion is retained; the mechanical protocol excludes only overlap-vetoed items, but no test is preregistered before repair.
- Fatal optimizer issue: uniform-mean RankShareMix prediction is affine in unrestricted ilr coordinates and therefore has infimum `-infinite` whenever its slope is nonzero. Selecting the minimum of `65536` positive Dirichlet candidates merely selects an extreme order statistic outside the `14`-row support; positivity alone does not define a scientific optimum.
- Fatal power/budget issues: with `5` unpaired seeds per arm, known-variance difference standard error is `sqrt(2/5)=0.6324555320336759` sigma and a one-sided `95%` bound at equality is approximately `1.04` sigma, so a `0.25`-sigma noninferiority margin is not powered. At `4` distinct final mixtures, the design needs `21+20=41` successful jobs; the `2`-hour per-job ceiling permits `82` MI210 node-hours, above the hard line cap `60`. The reviewer also requires matched ridge/scalar/rank-sweep/Olmix-`14` baselines.
- Disposition/evidence: no outcome was accessed and no H019 job was submitted. The only permitted repair must bound the optimizer, use a power-calibrated paired-seed design, close worst-case accounting below `60` node-hours, and strengthen same-budget attribution without relaxing the one-third swarm reduction. Every number/score is from `reviews/candidate_round6_pages.json` and full context-free review `reviews/candidate_round6.json`.

## H019 v2 - REJECTED AFTER SINGLE REPAIR; LINE STOPPED

- Date/type/levels: 2026-08-24, constructive, proposed staged L0/L1; this is repair `1/1` of H019 v1. The repair bounded every selector to `|ilr_j|<=2.5` and `KL(p||uniform)<=0.7`, increased paired final evaluation to `12` seeds, and capped the successful-job plan at `72` jobs times `0.75` hours = `54` MI210 node-hours, leaving `6` of the line's `60` node-hours for failed attempts.
- Calibration/review: published RegMix and DoGE scored N/F/I `8/9/9` and `8/8/8`; H019 v2 scored `5/5/4`, REJECT, with `5` nearest works, `3` strongest objections, an unresolved fatal objection, and no substantial-overlap veto. The single-repair allowance is exhausted.
- Fatal confirmation leak: every swarm response and all `12` paired final evaluations reuse the same fixed `51200` validation tokens per domain. Fresh training seeds separate model randomness but do not create an untouched evaluation estimand, so selector-induced finite-validation-set adaptation cannot support population or untouched-test quality.
- Fatal inference/claim mismatch: under the registered one-sided t rule with `12` pairs, the review computes equality power about `0.780`, below the page's `0.83`; an upper bound below `0.75` observed sigma permits positive degradation and therefore does not establish the registered compute-quality Pareto claim. The scalar-ridge penalty/tuning rule, fold-local LOO scaling and tie handling, Olmix Huber fit, optimizer-disagreement metric, Sobol stream, and SLSQP tolerances are also not frozen, so fallback admission cannot make the test uniquely executable after outcome access.
- Scope and feasibility audit: exact token arithmetic is `763*128*512=50003968`; the maximum plan is `3+21+4*12=72` successful jobs and `54` MI210 node-hours. These arithmetic gates pass, but the proposed roughly `19M`-parameter models receive about `50M` tokens versus the cited predecessor's `10B`, which limits the impact even if the missing definitions were supplied.
- Disposition/evidence: H019 is stopped without preregistration or execution; no public outcome cell was analyzed under H019, no Slurm test was submitted, test-job count is `0`, and GPU/node-hours are `0/0`. Every score, count, power calculation, and audit finding is in `reviews/candidate_round6_repair_pages.json` and the full context-free review `reviews/candidate_round6_repair.json`; no result artifact exists and this entry is not a scientific verdict in `{supported | falsified | inconclusive}`.

## H000 - H015 CONTINUATION FINALIZATION

- Structured summary: Slurm job `384598` ran `slurm/final_summary.sbatch` and completed in `00:00:00` with exit `0:0`. `scripts/build_final_summary.py` reports `10` completed L0 tests, verdict counts supported/falsified/inconclusive `5/3/2`, audit fraction `0.5`, literature expansion `15/15`, total GPU use `0/200`, and H015 candidate-line GPU use `0/60` MI210 node-hours in `artifacts/final_summary.json`.
- First validation attempt: Slurm job `384599` ran `slurm/final_check.sbatch` and failed in `00:00:01` with exit `1:0` because `scripts/check_final.py` accepted only the Unicode H015 candidate marker while RESULT used the existing ASCII H016 marker. The failed output recorded exactly `1` error, `H016: candidate-finding marker is missing`; no scientific input or verdict failed.
- Validator correction and confirmation: commit `0b1ac38` makes `scripts/check_final.py` accept both existing marker forms. Slurm job `384600` reran `slurm/final_check.sbatch` and completed in `00:00:01` with exit `0:0`; `artifacts/final_check.json` reports `passed=true`, `0` errors, `10` completed records, `5` supported IDs, and `3` closed IDs.
- Accounting/evidence: all `3` jobs used `0` MI210 GPUs and `0` MI210 node-hours. States, elapsed times, and exits were produced by `sacct -j 384598,384599,384600 --format=JobID,State,Elapsed,ExitCode -n -P`; inputs, commands, and outputs are recorded in `slurm/final_summary.sbatch`, `slurm/final_check.sbatch`, `artifacts/final_summary.json`, `artifacts/final_check.json`, and `/work1/ruixiangtang/rw761/data_mix_artifacts/slurm/final-{summary-384598,check-384599,check-384600}.{out,err}`.

## ONEDIAL-Q1 PREREGISTRATION

- ID/type/level: Q1, calibration, L0 with `0` GPUs. Discovery uses Olmix weight designs `m=6/12` and `200` independent simulations per each of `5` rank/zero scenarios; confirmation uses disjoint seeds and `m=18/24` with `400` simulations per scenario.
- Frozen decision: choose the H015-compatible `1e-6` ilr pipeline if it clears discovery, otherwise the zero-safe Hellinger pipeline if it clears discovery. Confirmation sees only the frozen winner. Support requires null false-positive rate/upper `95%` bound at most `0.075/0.10`, rank-`2` and rank-`5` detection rate/lower bound at least `0.80/0.75`, and rank-`2` loading cosine median/`5%` quantile at least `0.90/0.80`, separately for interior and structural-zero truth. Any confirmation miss falsifies; no discovery winner is `inconclusive` and quarantines real outcomes.
- Outcome quarantine: protocol design inspected file headers, row counts, source pins, and the previously published H015/H016 summaries, but computed no new One-Dial outcome statistic. Exact DGP, selection, death branch, and every number are in `protocol_onedial.json` `questions.Q1`; validation script `scripts/check_onedial_protocol.py`; inputs `PROTOCOL_ONEDIAL.md`, `protocol_onedial.json`, and the Olmix hash manifest; command `sbatch slurm/check_onedial_protocol.sbatch`.

## ONEDIAL-Q2 PREREGISTRATION

- ID/type/level: Q2, measurement, L0 with `0` GPUs. Discovery is `m=6/12`; confirmation is `m=18/24`; cross-fitted rank-`1` prediction is removed before testing residual spectra with `999` task-wise residual permutations and `2000` single-layer row bootstraps.
- Frozen decision: every scale requires `p_h<=0.01`, residual significant dimension in `[1,3]`, total/residual `h` energy point at least `0.05/0.50` with lower `95%` bounds at least `0.02/0.35`, cross-scale cosine point/lower bound at least `0.70/0.50`, and alternative-coordinate cosine at least `0.80` with dimension difference at most `1`. Exact One-Dial additionally requires dimension `1` and residual-energy lower bound at least `0.50`. A valid miss falsifies; declared validity or H015-g alignment failure is `inconclusive`.
- Evidence for every number and death branch: `scripts/check_onedial_protocol.py`; inputs `protocol_onedial.json` `questions.Q2`, `PROTOCOL_ONEDIAL.md`, and the eight files under `/work1/ruixiangtang/rw761/data_mix_public/olmix_rq2`; command `sbatch slurm/check_onedial_protocol.sbatch`.

## ONEDIAL-Q3 PREREGISTRATION

- ID/type/level: Q3, measurement, L0 with `0` GPUs. Discovery uses RegMix `1M` train, DataDecide sizes `4M` through `60M`, and Olmix `m=6/12`; confirmation uses RegMix `60M/1B`, DataDecide `90M` through `1B`, and Olmix `m=18/24`.
- Frozen decision: the Olmix/DataDecide exact bridge has `8` tasks and the RegMix/DataDecide macro bridge has `5` targets. Each external `h` requires permutation `p<=0.01` and residual energy at least `0.40`; both bridges require cosine point/lower `95%` bound at least `0.70/0.40` and sign agreement at least `6/8` or `4/5`, in discovery and confirmation. A valid miss falsifies; missing pinned DataDecide revision `b59512bb1b0b5dc02bfc469e0531be11149203d9`, any missing frozen target, or undefined `1e-8` polarity anchor is `inconclusive`.
- Frozen DataDecide transform: downstream loss is `-log(max(correct_prob_per_char,1e-12))`; validation loss is `log(perplexity)`. Select the maximum released step per recipe/model/task/seed, transform before averaging seeds, center recipe responses within model size, and use a fixed categorical one-hot recipe design. No alternate released metric may replace a missing column.
- Evidence for every number, mapping, and pin: `scripts/check_onedial_protocol.py`; inputs `protocol_onedial.json` `questions.Q3`, `vendor/regmix/data/*.csv`, `vendor/datadecide/perplexity_metrics_by_group.csv`, and `allenai/DataDecide-eval-results`; command `sbatch slurm/check_onedial_protocol.sbatch`.

## ONEDIAL-Q4 PREREGISTRATION

- ID/type/level: Q4, measurement, L0 with `0` GPUs. Discovery transfers axes `m=6 -> 12`; confirmation transfers `m=18 -> 24`. The target set has `512` vectors (`110` one-hot plus `402` Dirichlet at alpha `0.05`) split by hash before fitting; uncertainty uses `2000` single-layer target bootstraps and the control uses `999` task-residual shuffles.
- Frozen decision: held-out curve `R^2` point/lower `95%` bound must be at least `0.80/0.70`, shuffle `p<=0.01`, margin over shuffle `99%` quantile at least `0.10`, unique oracle mixtures at least `10`, and dial-angle span at least `0.75` radians in both stages. A valid miss falsifies; undersized halves, unresolved `1e-10` oracle ties, or unavailable Q2 dial is `inconclusive` unless Q2 was falsified, which yields `falsified_by_Q2`.
- Evidence for every number and branch: `scripts/check_onedial_protocol.py`; inputs `protocol_onedial.json` `questions.Q4`, `PROTOCOL_ONEDIAL.md`, and Olmix `m=6/12/18/24` official tables; command `sbatch slurm/check_onedial_protocol.sbatch`.

## ONEDIAL-Q5 PREREGISTRATION

- ID/type/level: Q5, constructive measurement, L0 with `0` GPUs. Discovery transfers axes `m=6 -> 12` with One-Dial/comparison budgets `26/39`; confirmation transfers `m=18 -> 24` with budgets `50/75`, implementing the frozen `2(m+1)` versus `3(m+1)` rule.
- Frozen decision: top-decile rate point/lower `95%` bound at least `0.80/0.70`; median IQR regret point/upper bound at most `0.10/0.15`; paired regret upper bound versus same/larger budget at most `0.02/0.05`; at least `50` candidates; and improvement over scrambled task loadings point/lower bound at least `0.05/0.02`, in both stages. A valid miss falsifies; rank deficiency, IQR at most `1e-8`, or missing source axes is `inconclusive` unless Q2 was falsified.
- Scope/evidence: this tests retrospective observation efficiency, not new training or scale transfer. Every number and branch is in `protocol_onedial.json` `questions.Q5`; validation script `scripts/check_onedial_protocol.py`; inputs `PROTOCOL_ONEDIAL.md` and official Olmix tables; command `sbatch slurm/check_onedial_protocol.sbatch`.

## ONEDIAL OVERALL PREREGISTRATION

- Overall decision: `SURVIVED` requires all `5` questions supported plus Q2 exact dimension `1`; `KILLED` takes precedence when any of Q1-Q4 is falsified; `PARTIAL` applies when no Q1-Q4 result is falsified but at least `1` question is inconclusive, Q5 is falsified, or Q2 is within the user ceiling `3` but not exact dimension `1`.
- Mutation rule: after user confirmation, no number, split, mapping, estimator, or branch may change. A protocol defect is appended here and that Q stops. Each second-act stage commits as `ledger: Q<n> <verdict>`.
- Evidence for every count and label: `scripts/check_onedial_protocol.py`; inputs `protocol_onedial.json` and `PROTOCOL_ONEDIAL.md`; command `sbatch slurm/check_onedial_protocol.sbatch`.

## ONEDIAL ACT I STATIC VALIDATION

- Protocol check: Slurm job `384637` completed in `00:00:00` with exit `0:0`. The checker reports `passed=true`, `0` errors, `5` preregistered questions, `6` explicitly marked agent-added checks, bootstrap layers `1`, nested bootstrap `false`, GPU count `0`, and preexisting One-Dial outcome artifacts `0`.
- Frozen identities: `protocol_onedial.json` SHA256 is `43a05296625f725b091b7e0a8a2003987e23b7879cc0ac019a2f4581f11bf494`; `PROTOCOL_ONEDIAL.md` SHA256 is `10ec812d2feebdb33a336a819c62fcdc550e6bb2ddeb221ebbecc59d98a34659`. The ledger hash `59552dc1475d20d6ba8ce541ad685cad3226a5700c1b8bec22d942a34806462c` identifies the append-only state immediately before this validation result was appended.
- Accounting/evidence: the job requested no GPU and used `0` MI210 node-hours. Every number and hash was produced by `scripts/check_onedial_protocol.py` from `protocol_onedial.json`, `PROTOCOL_ONEDIAL.md`, and the pre-result `EXPERIMENTS.md`; command `sbatch slurm/check_onedial_protocol.sbatch`; output `artifacts/onedial_protocol_check.json`; job state command `sacct -j 384637 --format=JobID,State,Elapsed,ExitCode -n -P`; raw log `/work1/ruixiangtang/rw761/data_mix_artifacts/slurm/onedial-protocol-check-384637.out`.

## ONEDIAL-Q1 INCONCLUSIVE - PROTOCOL DEFECT; QUESTION STOPPED

- ID/date/type/level/verdict: Q1, 2026-08-24, calibration, L0, `inconclusive`. The frozen truth block contains `3` interior scenarios and `1` structural-zero generator, but contains `0` interior generators. This count is produced by `scripts/audit_onedial_q1_design.py`; inputs `protocol_onedial.json` and the four official Olmix ratio tables; command `sbatch slurm/q1_design_audit.sbatch`; output `/work1/ruixiangtang/rw761/data_mix_artifacts/ONEDIAL/Q1/design_audit.json`.
- Executability audit: the official `m=6` table has `125` finite rows, of which `5` are strictly positive; their centered-log rank is `4` against required coordinate rank `5`. The official `m=12/18/24` tables have `130/129/127` finite rows and `0/0/0` strictly positive rows; their centered-log ranks are `0/0/0` against required ranks `11/17/23`. The exact-zero cell counts are `349/1162/1911/2634`, and rows containing at least one exact zero are `120/130/129/127`. Every count is produced by `scripts/audit_onedial_q1_design.py`; inputs `/work1/ruixiangtang/rw761/data_mix_public/olmix_rq2/m{6,12,18,24}_ratios.csv`; command `sbatch slurm/q1_design_audit.sbatch`; output `/work1/ruixiangtang/rw761/data_mix_artifacts/ONEDIAL/Q1/design_audit.json`.
- Defect and disposition: the public designs therefore cannot instantiate the declared interior ranks from their strictly positive rows, while the approved protocol does not define an interior transformation, replacement, or separate design generator. Choosing one after Act I would change the DGP and violate the immutable mutation rule. Q1 stops before synthetic calibration; pipelines A/B are not selected; real outcome tables read under One-Dial equal `0`. The Boolean defect and outcome-access count are produced by `scripts/audit_onedial_q1_design.py`; inputs and command are the same as the preceding audit paragraph.
- Job/accounting: job `384689` failed after `00:00:02` with exit `1:0` because the audit script initially addressed `dgp` rather than the frozen JSON field `truth`; commit `0f25abe` corrected only that path. Job `384704` then completed after `00:00:02` with exit `0:0`. Both jobs requested `0` GPUs and used `0` MI210 node-hours. States, elapsed times, and exits are produced by `sacct -j 384689,384704 --format=JobID,State,Elapsed,ExitCode,AllocTRES -n -P`; scripts/inputs/commands are `scripts/audit_onedial_q1_design.py`, `slurm/q1_design_audit.sbatch`, the frozen protocol, and the four official ratio tables.

## ONEDIAL-Q2 INCONCLUSIVE_BY_Q1 - DEPENDENCY BRANCH

- ID/date/type/level/verdict: Q2, 2026-08-24, measurement, L0, `inconclusive`; dependency state `inconclusive_by_Q1`. The frozen Q1 death branch blocks Q2 because Q1 selected `0` coordinate pipelines. These values are produced by `scripts/resolve_onedial_dependencies.py`; inputs `protocol_onedial.json` and `/work1/ruixiangtang/rw761/data_mix_artifacts/ONEDIAL/Q1/design_audit.json`; command `sbatch slurm/resolve_onedial_dependencies.sbatch`; output `/work1/ruixiangtang/rw761/data_mix_artifacts/ONEDIAL/dependency_resolution.json`.
- Result/disposition: real outcome tables read, downstream test jobs submitted, residual permutations run, and row bootstraps run are all `0`; the preregistered Q2 thresholds therefore remain unevaluated and no statement about residual `h` or dimension is made. The first two counts are emitted by `scripts/resolve_onedial_dependencies.py` from the preceding inputs and command; the latter two follow from the same output's dependency branch and `0` submitted downstream jobs.
- Dependency-job accounting: job `384706` failed after `00:00:01` with exit `1:0` because the resolver initially addressed `overall_decision` rather than the frozen field `overall_verdict`; commit `09dc599` corrected only that path. Job `384707` completed after `00:00:00` with exit `0:0`. Both requested `0` GPUs and used `0` MI210 node-hours. States, elapsed times, exits, and allocation are produced by `sacct -j 384706,384707 --format=JobID,State,Elapsed,ExitCode,AllocTRES -n -P`; code/input/command are `scripts/resolve_onedial_dependencies.py`, `protocol_onedial.json`, the Q1 audit JSON, and `sbatch slurm/resolve_onedial_dependencies.sbatch`.

## ONEDIAL-Q3 INCONCLUSIVE_BY_Q1 - DEPENDENCY BRANCH

- ID/date/type/level/verdict: Q3, 2026-08-24, measurement, L0, `inconclusive`; dependency state `inconclusive_by_Q1`. Q1 selected `0` coordinate pipelines, so the frozen death branch prohibits opening the RegMix, DataDecide, or Olmix outcome tables for this question. The state and count are produced by `scripts/resolve_onedial_dependencies.py`; inputs `protocol_onedial.json` and `/work1/ruixiangtang/rw761/data_mix_artifacts/ONEDIAL/Q1/design_audit.json`; command `sbatch slurm/resolve_onedial_dependencies.sbatch`; output `/work1/ruixiangtang/rw761/data_mix_artifacts/ONEDIAL/dependency_resolution.json`.
- Result/disposition/accounting: bridge cosines, polarity agreements, permutation p-values, and residual energies are unevaluated; Q3 submitted `0` downstream test jobs and consumed `0` MI210 node-hours. The submitted-job count and dependency state are produced by `scripts/resolve_onedial_dependencies.py` from the preceding inputs and command; job `384707` produced the dependency output with no GPU request, as recorded by `sacct -j 384707 --format=JobID,State,Elapsed,ExitCode,AllocTRES -n -P`.

## ONEDIAL-Q4 INCONCLUSIVE_BY_Q1 - DEPENDENCY BRANCH

- ID/date/type/level/verdict: Q4, 2026-08-24, measurement, L0, `inconclusive`; dependency state `inconclusive_by_Q1`. Q1 selected `0` coordinate pipelines, so no registered dial axes exist and the frozen death branch takes precedence before the Q4 objective split. The state and count are produced by `scripts/resolve_onedial_dependencies.py`; inputs `protocol_onedial.json` and `/work1/ruixiangtang/rw761/data_mix_artifacts/ONEDIAL/Q1/design_audit.json`; command `sbatch slurm/resolve_onedial_dependencies.sbatch`; output `/work1/ruixiangtang/rw761/data_mix_artifacts/ONEDIAL/dependency_resolution.json`.
- Result/disposition/accounting: held-out curve fit, shuffle control, oracle uniqueness, and angle span are unevaluated; Q4 submitted `0` downstream test jobs and consumed `0` MI210 node-hours. The submitted-job count and dependency state are produced by `scripts/resolve_onedial_dependencies.py` from the preceding inputs and command; job `384707` produced the dependency output with no GPU request, as recorded by `sacct -j 384707 --format=JobID,State,Elapsed,ExitCode,AllocTRES -n -P`.

## ONEDIAL-Q5 INCONCLUSIVE_BY_Q1 - DEPENDENCY BRANCH

- ID/date/type/level/verdict: Q5, 2026-08-24, constructive measurement, L0, `inconclusive`; dependency state `inconclusive_by_Q1`. Q1 selected `0` coordinate pipelines, so no frozen One-Dial pilot coordinate exists and the frozen death branch prevents retrospective selector fitting. The state and count are produced by `scripts/resolve_onedial_dependencies.py`; inputs `protocol_onedial.json` and `/work1/ruixiangtang/rw761/data_mix_artifacts/ONEDIAL/Q1/design_audit.json`; command `sbatch slurm/resolve_onedial_dependencies.sbatch`; output `/work1/ruixiangtang/rw761/data_mix_artifacts/ONEDIAL/dependency_resolution.json`.
- Result/disposition/accounting: top-decile rate, normalized regret, same-budget comparisons, larger-budget comparisons, and scrambled-loading attribution are unevaluated; Q5 submitted `0` downstream test jobs and consumed `0` MI210 node-hours. The submitted-job count and dependency state are produced by `scripts/resolve_onedial_dependencies.py` from the preceding inputs and command; job `384707` produced the dependency output with no GPU request, as recorded by `sacct -j 384707 --format=JobID,State,Elapsed,ExitCode,AllocTRES -n -P`.

## ONEDIAL ACT II FINAL VALIDATION

- Bundle result: overall verdict `PARTIAL`; question count `5`; inconclusive question count `5`; downstream questions blocked by Q1 `4`; real outcome tables read `0`; submitted downstream test jobs `0`; validation errors `0`; `passed=true`. Every count and state is produced by `scripts/check_onedial_final.py`; inputs `protocol_onedial.json`, `PROTOCOL_ONEDIAL.md`, `EXPERIMENTS.md`, `RESULT.md`, `BLOCKED.md`, `/work1/ruixiangtang/rw761/data_mix_artifacts/ONEDIAL/Q1/design_audit.json`, and `/work1/ruixiangtang/rw761/data_mix_artifacts/ONEDIAL/dependency_resolution.json`; command `sbatch slurm/check_onedial_final.sbatch`; output `/work1/ruixiangtang/rw761/data_mix_artifacts/ONEDIAL/final_check.json`.
- Protocol integrity: JSON SHA256 remains `43a05296625f725b091b7e0a8a2003987e23b7879cc0ac019a2f4581f11bf494` and Markdown SHA256 remains `10ec812d2feebdb33a336a819c62fcdc550e6bb2ddeb221ebbecc59d98a34659`, matching Act I. Both hashes are produced by `scripts/check_onedial_final.py` from `protocol_onedial.json` and `PROTOCOL_ONEDIAL.md`; command and output are the final-validation command and output above.
- Job/accounting: Slurm job `384717` completed after `00:00:01` with exit `0:0`, requested `0` GPUs, and used `0` MI210 node-hours. State, elapsed time, exit, and allocation are produced by `sacct -j 384717 --format=JobID,State,Elapsed,ExitCode,AllocTRES -n -P`; submitted script `slurm/check_onedial_final.sbatch`; result path and validation command are listed above.

## ONEDIAL-V2 Q1 SYNTHETIC-GENERATOR AMENDMENT - PREREGISTERED; EXECUTION PAUSED

- ID/scope/status: ONEDIAL-V2 revises exactly Q1's interior synthetic-weight definition after the confirmed ONEDIAL-V1 protocol defect; second-act execution remains not authorized. JSON comparison reports exactly `2` changed paths, `/protocol_id` and `/questions/Q1/truth/interior_weight_generator`. This count and path set are produced by `scripts/check_onedial_v2_amendment.py`; inputs `git:a456c6e:protocol_onedial.json`, `git:a456c6e:PROTOCOL_ONEDIAL.md`, `protocol_onedial.json`, and `PROTOCOL_ONEDIAL.md`; command `sbatch slurm/check_onedial_v2_amendment.sbatch`; outputs `artifacts/onedial_v2_amendment_check.json` and `artifacts/onedial_v1_to_v2.diff`.
- Frozen interior generator: for each phase, `m`, interior scenario, and zero-based replicate index, draw a fresh independent `n_m` by `m` matrix from symmetric Dirichlet alpha `1.0`; `n_m` is the finite weight-row count of the corresponding unmodified Olmix ratio table. NumPy PCG64 receives the little-endian uint64 from the first `8` SHA256 bytes of the exact label serialization in `protocol_onedial.json` under `questions.Q1.truth.interior_weight_generator.seed_rule`. The definition and all numeric fields are validated by `scripts/check_onedial_v2_amendment.py`; inputs, command, and output are the amendment-check inputs, command, and JSON above.
- Unchanged structural-zero branch: structural-zero scenarios still use the unmodified real Olmix design and the zero-safe Hellinger truth generator. The checker reports `q1_structural_zero_generator_unchanged=true`. This Boolean is produced by `scripts/check_onedial_v2_amendment.py` from the V1/V2 protocol inputs and command above.
- Noninterference proof: all `16` protected JSON sections have identical canonical hashes between V1 and V2, including Q2-Q5, common estimator, global randomness, inputs, thresholds, mappings, split definitions, execution order, and verdict branches; all `7` protected Markdown sections also have identical byte hashes. The unified diff contains `13` added and `3` removed lines and has SHA256 `025eddf302e709d6059398edb1e27c9038e03b8a2cc186ed524dcaed5f27d3e8`. Every count, Boolean, and hash is produced by `scripts/check_onedial_v2_amendment.py`; inputs, command, and outputs are listed above.
- Frozen-file identities: V1/V2 protocol JSON SHA256 values are `43a05296625f725b091b7e0a8a2003987e23b7879cc0ac019a2f4581f11bf494` and `e7cca5e5c0e8ef7405af03f8ff9bd458c33f0bcf6a9f3278ddad65e7565f414b`; V1/V2 protocol Markdown SHA256 values are `10ec812d2feebdb33a336a819c62fcdc550e6bb2ddeb221ebbecc59d98a34659` and `c936d9196cfd5f078c233fc58e2c1b022f47149306503715eae879eabc247dd1`. The hashes are produced by `scripts/check_onedial_v2_amendment.py` from the V1 Git objects and V2 working-tree files using the command above.
- Job/accounting: Slurm job `384742` completed after `00:00:01` with exit `0:0`, requested `0` GPUs, and used `0` MI210 node-hours. State, elapsed time, exit, and allocation are produced by `sacct -j 384742 --format=JobID,State,Elapsed,ExitCode,AllocTRES -n -P`; submitted script `slurm/check_onedial_v2_amendment.sbatch`; result and diff paths are listed above.

## ONEDIAL-V2 STATIC VALIDATION - PASSED; EXECUTION PAUSED

- Protocol check: Slurm job `384743` completed after `00:00:01` with exit `0:0`. The checker reports `passed=true`, `0` errors, protocol ID ONEDIAL-V2, `5` preregistered questions, `6` agent-added checks, bootstrap layers `1`, nested bootstrap `false`, GPU count `0`, and preexisting V2 outcome artifacts `0`. Every count and state is produced by `scripts/check_onedial_protocol.py`; inputs `protocol_onedial.json`, `PROTOCOL_ONEDIAL.md`, and the pre-result `EXPERIMENTS.md`; command `sbatch slurm/check_onedial_protocol.sbatch`; output `artifacts/onedial_protocol_check.json`.
- Frozen identities: V2 protocol JSON SHA256 is `e7cca5e5c0e8ef7405af03f8ff9bd458c33f0bcf6a9f3278ddad65e7565f414b`, V2 Markdown SHA256 is `c936d9196cfd5f078c233fc58e2c1b022f47149306503715eae879eabc247dd1`, and the ledger immediately before this validation entry has SHA256 `69debf7b21c8ebc6830d6513d9796ac5cd38985fc810667c764d5dfb47048bb7`. All `3` hashes are produced by `scripts/check_onedial_protocol.py` from the preceding inputs and command; output `artifacts/onedial_protocol_check.json`.
- Accounting/pause: job `384743` requested `0` GPUs and used `0` MI210 node-hours. State, elapsed time, exit, and allocation are produced by `sacct -j 384743 --format=JobID,State,Elapsed,ExitCode,AllocTRES -n -P`; second-act V2 test-job count remains `0` until explicit confirmation, as produced by `scripts/check_onedial_protocol.py` from the preceding inputs and command via `preexisting_outcome_artifact_count=0` and the Markdown execution-pause marker.

## ONEDIAL-V2-Q1 INCONCLUSIVE - HOLM RESOLUTION PROTOCOL DEFECT; QUESTION STOPPED

- ID/date/type/level/verdict: Q1, 2026-08-24, calibration, L0, `inconclusive`. The registered `999` permutations give denominator `1000` and minimum attainable p-value `0.001`, while Holm familywise alpha is `0.01`. These values are produced by `scripts/audit_onedial_q1_holm_resolution.py`; input `protocol_onedial.json`; command `sbatch slurm/q1_holm_resolution_audit.sbatch`; output `/work1/ruixiangtang/rw761/data_mix_artifacts/ONEDIAL_V2/Q1/holm_resolution_audit.json`.
- Design resolution: discovery `m=6` has residual family size `5`, first Holm threshold `0.002`, and is resolvable with at least `499` permutations. Discovery `m=12` has family size `11`, threshold `0.0009090909090909091`, needs at least `1099` permutations, and has a registered shortfall of `100`. Confirmation `m=18/24` has family sizes `17/23`, thresholds `0.0005882352941176471/0.0004347826086956522`, needs at least `1699/2299` permutations, and has shortfalls `700/1300`. Every count and threshold is produced by `scripts/audit_onedial_q1_holm_resolution.py`; input, command, and output are the Q1 Holm audit inputs, command, and output above.
- Defect and disposition: `3` of `4` designs cannot reject even the first residual factor under the exact registered detector, so their maximum possible significant residual dimension is `0`. Q1 requires all designs and rank-`2` detection rate at least `0.80`, but the mathematical upper bound at each impossible design is `0.0`. Increasing permutations or altering the Holm family after V2 freeze would change the detector; Q1 therefore stops before any synthetic realization is generated, pipelines selected equal `0`, and synthetic/real outcomes read equal `0/0`. All counts, bounds, Booleans, and outcome-access states are produced by `scripts/audit_onedial_q1_holm_resolution.py`; input, command, and output are listed above.
- Job/accounting: Slurm job `384756` completed after `00:00:01` with exit `0:0`, requested `0` GPUs, and used `0` MI210 node-hours. State, elapsed time, exit, and allocation are produced by `sacct -j 384756 --format=JobID,State,Elapsed,ExitCode,AllocTRES -n -P`; submitted script `slurm/q1_holm_resolution_audit.sbatch`; result path and command are listed above.

## ONEDIAL-V2-Q2 INCONCLUSIVE_BY_Q1 - DEPENDENCY BRANCH

- ID/date/type/level/verdict: Q2, 2026-08-24, measurement, L0, `inconclusive`; dependency state `inconclusive_by_Q1`. Q1 selected `0` coordinate pipelines after `3` mathematically impossible detector designs, so the frozen death branch blocks Q2. These counts and states are produced by `scripts/resolve_onedial_v2_dependencies.py`; inputs `protocol_onedial.json` and `/work1/ruixiangtang/rw761/data_mix_artifacts/ONEDIAL_V2/Q1/holm_resolution_audit.json`; command `sbatch slurm/resolve_onedial_v2_dependencies.sbatch`; output `/work1/ruixiangtang/rw761/data_mix_artifacts/ONEDIAL_V2/dependency_resolution.json`.
- Result/disposition: synthetic outcome realizations, real outcome tables, downstream test jobs, residual permutations, and row bootstraps run are all `0`; residual `h`, dimension, energy, stability, and exact-one statistics are therefore `n/a`. The first `3` counts are produced by `scripts/resolve_onedial_v2_dependencies.py` from the preceding inputs and command; the latter `2` zero counts and `n/a` statistics follow from the same dependency output's `0` submitted downstream jobs.
- Dependency-job accounting: Slurm job `384757` completed after `00:00:01` with exit `0:0`, requested `0` GPUs, and used `0` MI210 node-hours. State, elapsed time, exit, and allocation are produced by `sacct -j 384757 --format=JobID,State,Elapsed,ExitCode,AllocTRES -n -P`; submitted script `slurm/resolve_onedial_v2_dependencies.sbatch`; result path and command are listed above.

## ONEDIAL-V2-Q3 INCONCLUSIVE_BY_Q1 - DEPENDENCY BRANCH

- ID/date/type/level/verdict: Q3, 2026-08-24, measurement, L0, `inconclusive`; dependency state `inconclusive_by_Q1`. Q1 selected `0` coordinate pipelines, so the frozen death branch prohibits opening RegMix, DataDecide, or Olmix outcome tables for this question. The state and count are produced by `scripts/resolve_onedial_v2_dependencies.py`; inputs `protocol_onedial.json` and `/work1/ruixiangtang/rw761/data_mix_artifacts/ONEDIAL_V2/Q1/holm_resolution_audit.json`; command `sbatch slurm/resolve_onedial_v2_dependencies.sbatch`; output `/work1/ruixiangtang/rw761/data_mix_artifacts/ONEDIAL_V2/dependency_resolution.json`.
- Result/disposition/accounting: bridge cosines, polarity agreements, permutation p-values, and residual energies are `n/a`; Q3 submitted `0` downstream test jobs and consumed `0` MI210 node-hours. The submitted-job count and dependency state are produced by `scripts/resolve_onedial_v2_dependencies.py` from the preceding inputs and command; job `384757` produced the dependency output with no GPU request, as recorded by `sacct -j 384757 --format=JobID,State,Elapsed,ExitCode,AllocTRES -n -P`.

## ONEDIAL-V2-Q4 INCONCLUSIVE_BY_Q1 - DEPENDENCY BRANCH

- ID/date/type/level/verdict: Q4, 2026-08-24, measurement, L0, `inconclusive`; dependency state `inconclusive_by_Q1`. Q1 selected `0` coordinate pipelines, so no registered dial axes exist and the frozen death branch takes precedence before the objective split. The state and count are produced by `scripts/resolve_onedial_v2_dependencies.py`; inputs `protocol_onedial.json` and `/work1/ruixiangtang/rw761/data_mix_artifacts/ONEDIAL_V2/Q1/holm_resolution_audit.json`; command `sbatch slurm/resolve_onedial_v2_dependencies.sbatch`; output `/work1/ruixiangtang/rw761/data_mix_artifacts/ONEDIAL_V2/dependency_resolution.json`.
- Result/disposition/accounting: held-out curve fit, shuffle control, oracle uniqueness, and angle span are `n/a`; Q4 submitted `0` downstream test jobs and consumed `0` MI210 node-hours. The submitted-job count and dependency state are produced by `scripts/resolve_onedial_v2_dependencies.py` from the preceding inputs and command; job `384757` produced the dependency output with no GPU request, as recorded by `sacct -j 384757 --format=JobID,State,Elapsed,ExitCode,AllocTRES -n -P`.

## ONEDIAL-V2-Q5 INCONCLUSIVE_BY_Q1 - DEPENDENCY BRANCH

- ID/date/type/level/verdict: Q5, 2026-08-24, constructive measurement, L0, `inconclusive`; dependency state `inconclusive_by_Q1`. Q1 selected `0` coordinate pipelines, so no frozen One-Dial pilot coordinate exists and the death branch prevents retrospective selector fitting. The state and count are produced by `scripts/resolve_onedial_v2_dependencies.py`; inputs `protocol_onedial.json` and `/work1/ruixiangtang/rw761/data_mix_artifacts/ONEDIAL_V2/Q1/holm_resolution_audit.json`; command `sbatch slurm/resolve_onedial_v2_dependencies.sbatch`; output `/work1/ruixiangtang/rw761/data_mix_artifacts/ONEDIAL_V2/dependency_resolution.json`.
- Result/disposition/accounting: top-decile rate, normalized regret, same-budget comparisons, larger-budget comparisons, and scrambled-loading attribution are `n/a`; Q5 submitted `0` downstream test jobs and consumed `0` MI210 node-hours. The submitted-job count and dependency state are produced by `scripts/resolve_onedial_v2_dependencies.py` from the preceding inputs and command; job `384757` produced the dependency output with no GPU request, as recorded by `sacct -j 384757 --format=JobID,State,Elapsed,ExitCode,AllocTRES -n -P`.

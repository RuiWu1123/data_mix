# Assumption Audit

Pending complete paper-and-code audit. Every eventual entry is validated by `python scripts/check_assumptions.py ASSUMPTIONS.md --minimum-methods 5 --maximum-method-share 0.4`; input is this file and the thresholds are mirrored from `protocol.json`.

# Blocked Lines

Lines are appended here only when a required public artifact is absent or contradicts the paper. Inventory evidence is generated by `scripts/reference_inventory.py`; inputs `references/*.pdf` and `vendor/*/.git`; command `python scripts/reference_inventory.py --references references --vendor vendor --output artifacts/reference_inventory.json`.

## B-001 MDE

The paper provides no official code link, and title, author, and identifier searches found no author-controlled repository or public expert logits/loss table. The fixed-gate counterfactual line is stopped. Evidence inputs: `references/mde_2025_acl.pdf`, `artifacts/reference_inventory.json`; inventory command above.

## B-002 InfoLaw

No official code, run-level numeric table, checkpoints, recipe-search range, or seed record is linked by the paper. Exact cross-recipe and cross-overtraining tests are stopped. Evidence inputs: `references/infolaw_2605.02364.pdf`, `artifacts/reference_inventory.json`; inventory command above.

## B-003 RegMix-D

No official repository, proxy trajectories, learned mixtures, or run-level results are linked by the paper. Trajectory-state sufficiency tests are stopped. Evidence inputs: `references/regmix_d_2606.18663.pdf`, `artifacts/reference_inventory.json`; inventory command above.

## B-004 Mixture pretraining under data constraints

No official code, loss table, configuration, or checkpoint artifact is linked by the paper. Grouped-holdout and repetition-sufficiency tests are stopped. Evidence inputs: `references/scaling_mixture_constraints_2605.12715.pdf`, `artifacts/reference_inventory.json`; inventory command above.

## B-005 Scaling domain data repetition

No official code, run-level table, exact model configuration, or checkpoint artifact is linked by the paper. Cross-scale optimum-transfer tests are stopped. Evidence inputs: `references/scaling_domain_repetition_2608.14071.pdf`, `artifacts/reference_inventory.json`; inventory command above.

## B-006 Data-constrained pretraining scaling laws

The repository contains training code but omits the SoftQ fitting implementation, run-level numeric table, result payloads, and checkpoints required for nested scale holdout. The SoftQ model-comparison line is stopped. Evidence inputs: `references/data_constrained_2606.06888.pdf`, `vendor/dc_pretrain`, `artifacts/reference_inventory.json`; inventory command above.

## B-007 H009 Olmix 1B normalization confirmation

The four official RQ2 releases contain `511` row-level run names, all identifying 30M proxy runs and `0` identifying 1B runs. The paper reports 1B behavior only in aggregated Figure 18, without the per-mixture, per-seed target table required for the frozen common-oracle and `10000`-bootstrap confirmation. H009 is stopped rather than reconstructed from plot pixels. Evidence: `scripts/audit_h009_artifacts.py`; inputs `/work1/ruixiangtang/rw761/data_mix_public/olmix_rq2` and `/work1/ruixiangtang/rw761/data_mix_artifacts/paper_text/olmix_2602.12237.txt`; command `sbatch slurm/h009_availability.sbatch`; result `/work1/ruixiangtang/rw761/data_mix_artifacts/H009/availability.json`; Slurm job `384399`.

## B-008 One-Dial Q1 interior-DGP protocol defect

The approved Q1 truth block declares `3` interior scenarios but contains `0` interior generators. The official `m=6/12/18/24` tables supply `5/0/0/0` strictly positive rows, whose centered-log ranks are `4/0/0/0` against required ranks `5/11/17/23`. The frozen designs therefore cannot instantiate the declared interior rank scenarios from strictly positive public rows. ONEDIAL-V1 is stopped rather than retroactively adding a closure, replacement, or new design generator; Q2-Q5 take the preregistered `inconclusive_by_Q1` branch and real outcome tables read equal `0`. Every count and branch is produced by `scripts/audit_onedial_q1_design.py` and `scripts/resolve_onedial_dependencies.py`; inputs `protocol_onedial.json`, the four official ratio tables, and `/work1/ruixiangtang/rw761/data_mix_artifacts/ONEDIAL/Q1/design_audit.json`; commands `sbatch slurm/q1_design_audit.sbatch` and `sbatch slurm/resolve_onedial_dependencies.sbatch`; outputs `/work1/ruixiangtang/rw761/data_mix_artifacts/ONEDIAL/Q1/design_audit.json` and `/work1/ruixiangtang/rw761/data_mix_artifacts/ONEDIAL/dependency_resolution.json`.

## B-009 ONEDIAL-V2 Q1 Holm resolution defect

The frozen detector uses `999` permutations and p-value denominator `1000`, so its minimum attainable p-value is `0.001`. For residual family sizes `11/17/23` at `m=12/18/24`, Holm's first thresholds are `0.0009090909090909091/0.0005882352941176471/0.0004347826086956522`; no observed statistic can reject even the first residual factor. The detector would need at least `1099/1699/2299` permutations for numerical resolution, but changing the registered count after authorization is forbidden. ONEDIAL-V2 Q1 stops as `inconclusive`; Q2-Q5 take `inconclusive_by_Q1`, with synthetic realizations and real outcome tables read equal `0/0`. Every count and branch is produced by `scripts/audit_onedial_q1_holm_resolution.py` and `scripts/resolve_onedial_v2_dependencies.py`; inputs `protocol_onedial.json` and `/work1/ruixiangtang/rw761/data_mix_artifacts/ONEDIAL_V2/Q1/holm_resolution_audit.json`; commands `sbatch slurm/q1_holm_resolution_audit.sbatch` and `sbatch slurm/resolve_onedial_v2_dependencies.sbatch`; outputs `/work1/ruixiangtang/rw761/data_mix_artifacts/ONEDIAL_V2/Q1/holm_resolution_audit.json` and `/work1/ruixiangtang/rw761/data_mix_artifacts/ONEDIAL_V2/dependency_resolution.json`.
